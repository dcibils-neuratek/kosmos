# Kosmos

A microkernel desktop operating system with a userland written entirely in Lua.

A personal learning project. No users to serve, no compatibility to maintain, no deadline. That is precisely what makes it possible to take decisions a commercial OS cannot take.

**A desktop, a filesystem with a journal, a TCP/IP stack and a web
browser**, on a microkernel that knows about threads, address spaces, IPC
and capabilities and nothing else. [docs/roadmap.md](docs/roadmap.md) is
what is built and what is wanted; [docs/state.md](docs/state.md) is where it
actually is this week, and [docs/architecture.md](docs/architecture.md) is
how the layers fit together.

There are no milestones. There were thirteen, and the numbers went on being
quoted long after they stopped describing anything - this line said "M6,
graphics" while the machine had a browser in it.

---

There is a page about it at
[dcibils-neuratek.github.io/kosmos](https://dcibils-neuratek.github.io/kosmos/),
which is [`docs/index.html`](docs/index.html) - GitHub Pages will serve the
repository root or `/docs` and nothing else, so the site lives beside the
design documents rather than in a folder of its own.

## Run it

One command. Nothing to clone, nothing to build, no toolchain:

```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/dcibils-neuratek/kosmos/main/get-and-run-kosmos.sh)" -- -b "wm"
```

You need QEMU, and it is the only thing this cannot fetch for you:
`brew install qemu` on macOS, `apt install qemu-system-arm` on Debian or
Ubuntu. Everything else - the newest image, the runner, a disk to keep files
on - it works out and downloads.

`-r 1280x720` asks for the smaller build, because the framebuffer size is
compiled in and a different resolution is a different image. On **Windows**,
use WSL and the same command, or run QEMU natively with the flags
[the website](https://dcibils-neuratek.github.io/kosmos/) spells out - the
shell script will not run there. Only macOS is actually tested.

That boots straight to the desktop. Some other things to try once it is up,
or to pass instead of `wm`:

```sh
-b "wm browser"      the browser, on a page inside the image
-b "wm tracker"      the file manager
-b "wm cube3d"       a spinning cube, in software, at 50 fps
-b "wm doom"         Doom, once there is a WAD at /home/doom1.wad
-serial              no window at all, just the shell on this terminal
```

**Nothing asks you to know a file name.** A release is called something like
`kosmos-0.8.34-8c8c1f5-1920x1080-full.elf` and the version and commit in it
change every time, so the published list is asked for and the newest worked
out. `--list` shows what there is.

`sh -c "$(curl ...)"` rather than `curl | sh`, and the difference is not
style: a pipe *is* the script's stdin, and the last thing it does is hand
over to QEMU with `-serial mon:stdio` - so the guest's console would read
from an exhausted pipe and see end-of-file the moment it started.

Keeping a copy of the script works too and is shorter to type afterwards.
It will tell you when it differs from the published one.

---

## The idea in one paragraph

**BeOS is the reference**, and not one influence among several: the desktop is drawn from it, and so is the standard it is held to. The tab as wide as its title, the Deskbar, replicants, typed attributes with live queries over them, and responsiveness as a design constraint rather than an optimisation.

What is taken from elsewhere is what BeOS did not have an answer for: the microkernel from QNX, per-process namespaces from Plan 9, capabilities from seL4, the live image from Lisp Machines. All of it on top of a userland written in Lua.

What holds it together is one rule about what travels between things: **the language's data model where the shape is the caller's to choose, a declared struct where the shape is agreed.**

So IPC messages are Lua tables almost everywhere — the shell, applications, the UI kit, scripting, the window manager — and that is what gives the system one mentality from the prompt upward. At the boundary into a system server they are structs declared in a header both sides compile against, for two reasons found by building it: a server has to stay correct when its caller is wrong, and a garbage collector cannot sit where something else's timing depends on it. `docs/design.md` §1 has the argument and what it costs.

---

## Why it exists

Three reasons, in order.

**Learning by building.** Writing a kernel is the only way to actually understand what an OS does. Everything else is reading about it.

**Testing ideas that exist but never appear together.** Every piece of Kosmos worked in some real system. None of them had all of it at once, almost always because of commercial constraints that do not apply here.

**Having a first-hand answer to a concrete question:** how much does a system simplify if every resource speaks one protocol and every process sees only what you mounted for it?

On Linux, reading a file, reading a sensor, listing processes, watching for changes and reading metadata are five different APIs. On Kosmos it is one. That is the entire argument.

---

## What it is NOT

**It is not a daily driver, and chasing that would kill the project.** A daily driver needs a browser, and a modern browser is 30 million lines that assume POSIX, threads, a JIT, a GPU and a full network stack. Porting Chromium is more work than the whole system. That was the wall Haiku hit, with 20 years and dozens of contributors.

**It is not a BeOS clone.** BeOS was monolithic, with drivers and the filesystem inside the kernel. Kosmos takes its concurrency model, its live queries and its design sensibility. The architecture is QNX.

**It does not pursue system-level compatibility.** Computational libraries (SQLite, zlib, monocypher, Doom) can be ported with a libc shim inside the process, subject to its capabilities. What does not fit is a POSIX personality: `fork`, signals, BSD sockets, or a global tree. The moment that exists, the namespace design becomes decorative and the experiment loses its result.

---

## The verifiable goal

**Doom running at 35fps in an app server window, on a Raspberry Pi, next to a REPL where you redefine the window manager while you play.**

Doom is the best OS design benchmark there is: it needs no GPU and no floats on the hot path, but it does need a framebuffer with a fast blit, low-latency input, precise timing, real file reads and app-managed memory. If it runs smooth, the system is sound. If it stutters, there is a real and locatable problem.

---

## The seven principles

If something collides with one of these, the feature gets cut, not the principle.

1. **The kernel does not know what a file is.** Threads, address spaces, IPC, capabilities. Nothing else.
2. **What a process has not mounted does not exist.** Not permission denied — no such path.
3. **One protocol for every resource.** Sensor, process, file, window, network connection.
4. **Share-nothing userland.** One `lua_State` per process. Concurrency complexity stays confined to the kernel.
5. **Isolation comes from the hardware.** An EL0 address space plus capabilities. Lua does not need to be sandboxed.
6. **The system is modified while running.** ~~A server reloads its code without losing state or clients.~~ **Withdrawn September 2026**: every server is C, there is no dynamic linking, and hot reload was removed with the last Lua one. See `docs/design.md` §10.
7. **Compatibility inside a process, never at system level.** A libc inside an app is necessary and fine. A POSIX personality (`fork`, signals, a global tree, ambient authority) is forbidden. The line is drawn in [design.md](docs/design.md) section 17.

---

## Document map

| Document | What it answers |
|---|---|
| [docs/architecture.md](docs/architecture.md) | **The layers, and one command traced through all of them.** The place to start. |
| [docs/design.md](docs/design.md) | What Kosmos is and **why every decision is the way it is**. The central document. |
| [docs/ui.md](docs/ui.md) | UI kit and window manager. BeOS lineage with the corrections the design allows. |
| [docs/beos.md](docs/beos.md) | The BeOS lineage: what is inherited, what is corrected, and where Kosmos departs on purpose. |
| [docs/gfx.md](docs/gfx.md) | The path pixels take from Lua to the framebuffer, for apps that produce images. |
| [docs/testing.md](docs/testing.md) | How each layer is measured and how regressions are caught. |
| [docs/hal.md](docs/hal.md) | How `arch/` is separated from `hal/`, the targets, and the trap in each piece of hardware. |
| [docs/roadmap.md](docs/roadmap.md) | What is built, and the wishlist. |
| [docs/setup.md](docs/setup.md) | Toolchain, build, how to debug without a debugger. |
| [docs/glossary.md](docs/glossary.md) | The terms. Written to be re-read six months from now. |
| [docs/state.md](docs/state.md) | Where you are today. **Updated at the end of every session.** |
| [CLAUDE.md](CLAUDE.md) | Operating rules for working with Claude Code. |

Reading order if you come back after months: `state.md`, then `design.md`, then whichever one matches the current stage.

---

## Building it

[Running it](#run-it) needs none of this. To build:

```
make qemu
```

That is the whole system at 1920x1080 - browser, Doom, network, every demo.
`FULL=0` gives a leaner image. Toolchain and prerequisites in
[docs/setup.md](docs/setup.md).

---

## Layout

```
boot/           assembly entry, linker script
arch/           aarch64/ — what depends on the CPU
hal/            qemu-virt/ — what depends on the board (pi5/, pi1/ at M2)
kernel/         EL1: mmu, sched, ipc, caps, exceptions
lua/            upstream/ untouched + kosmos/ for freestanding
runtime/        minimal libc, and upstream/ for what is vendored
assets/         vendored data: fonts, icons, images, with their licences
user/           everything at EL0:
  init/           init, the roles, and the namespace kit
  servers/        the servers, in C: audio, devices, binfs, appfs,
                  console, ramfs
  include/        the protocol headers both sides compile against
  lib/            libraries: Lua (ui, theme, pdf) and C kits (gfx, gl,
                  console, png)
  bin/            programs and applications, in Lua
  tests/          the Lua suite
tests/          guest-side tests, in C
bench/          benchmarks and baselines.json
tools/          host-side test runner, scripts
docs/
book/
```

---

## The criterion for every future decision

**Does this test an idea in the design, or does it just close a feature gap against Linux?**

A filesystem with attributes tests an idea. A Chromium port closes a gap. The first is why the project started. The second is why projects get abandoned.

---

## Decision log

Design decisions taken outside the documents get recorded here before being propagated to `design.md` and `roadmap.md`.

| Date | Decision | Where it landed |
|---|---|---|
| Aug 2026 | Vanilla Lua 5.4 as the userland language, LuaJIT rejected | design.md §5 |
| Aug 2026 | Freestanding C11 for the kernel, Rust rejected | design.md §6 |
| Aug 2026 | Command-based drawing model (B), shared memory only as an exception | design.md §7.4 |
| Aug 2026 | UI kit with follow modes, no constraint solver | ui.md §16.4 |
| Aug 2026 | Target order: QEMU → Pi 1 → Pi 5 → Jetson → G4. Mac Pro 2013 rejected (no serial) | hal.md |
| Aug 2026 | Doom as the project's verifiable goal | roadmap.md M10 |
| Aug 2026 | The POSIX line: a libc inside the process yes, a POSIX personality no | design.md §17 |
| Aug 2026 | A server pumps while it waits: `ipc_receive` takes a non-blocking flag, rather than a second thread inside a server | design.md §7, architecture.md §2 |
| Aug 2026 | Who gets Control-C is decided by who is reading the keyboard: foreground program, else the line editor. Background is left alone | architecture.md §3 |
| Aug 2026 | Interruption is cooperative and stays that way until a process can be ended from outside | roadmap.md M6 |
| Aug 2026 | A program hands capabilities to its children under names: `run(path, args, detach, shares)` | ui.md §16.7, architecture.md §2 |
| Aug 2026 | The idle thread yields when anything is runnable and sleeps only when nothing is. Every yield used to cost a timer period | testing.md, kernel/main.c |
| Aug 2026 | Latency between operations is measured separately from operations. `bench/` is structurally blind to it | testing.md |
| Aug 2026 | A live query is a parked reply, not a callback: a server that calls a client can be blocked by one | design.md §7, roadmap.md M7 |
| Aug 2026 | The UI kit takes BeOS's structure and not its skin: copy a decision about behaviour, decide a decision about shading fresh | ui.md §16.8b |
| Aug 2026 | An editor early, before the UI kit: nothing else lets the machine change itself without a rebuild | roadmap.md, recorded goals |
| Aug 2026 | The window manager reserves one key and it introduces a command, rather than reserving Tab and the arrows | ui.md §16.7 |
| Aug 2026 | A library is a file in the namespace, loaded into the caller's environment. No package path, no module table | ui.md, init.lua `use` |
| Aug 2026 | An app is scriptable because it used `ui.window`, not because it wrote scripting code | roadmap.md M7, ui.md §16.7 |
| Aug 2026 | The `/app` registry hands out capabilities and never forwards: a forwarding registry is one process any app could stop | init.lua, architecture.md §2 |
| Aug 2026 | A directory whose children are looked up on demand (`mount_registry`), so a namespace can hold things that come and go | init.lua `resolve` |
| Aug 2026 | A replicant is Lua source + state + a `needs` list, restricted by the language rather than by the kernel, and it says so | ui.md §16.8 |
| Aug 2026 | An absolute pointing device, not a relative one: no acceleration curve to agree on with the host, so the guest cursor cannot drift from the real one | hal.md |
| Aug 2026 | The pointer's position leaves the HAL in the device's own units with its range beside it. Only the window manager knows the screen size, so only it scales | hal.md, CLAUDE.md |
| Aug 2026 | A process that owns the screen takes it from the kernel console, which falls back to the serial line. A panic takes it back | kernel/console.c, architecture.md |
| Aug 2026 | A press grabs, at both layers: the window it landed in, and the view inside that window, until the release | ui.md §16.7 |
| Aug 2026 | Hover is not forwarded to applications; movement is, but only while a button is held | ui.md §16.7 |
| Aug 2026 | The cursor is part of the composite, not drawn over it afterwards | ui.md §16.7 |
| Aug 2026 | A window's damage waits until its drawing is complete, so a frame is never seen half-redrawn | ui.md §16.7 |
| Aug 2026 | A program declares it draws with `-- kosmos: application`; the store reports it. The launcher does not guess by reading source | init.lua binfs |
| Aug 2026 | The Deskbar lists the desktop's windows, not the `/app` registry: a window with no registration is still a window | deskbar.lua |
| Aug 2026 | A kill marks and unblocks; the process dies at its own next entry into the kernel, because teardown ends with the thread that performs it | process.c, trap.c |
| Aug 2026 | Only a parent may end a child - the authority `wait` already implies, and no new one | syscall.c |
| Aug 2026 | A close box asks first and ends second: an application that listens leaves tidily, one that does not is ended a second later | wm.lua |
| Aug 2026 | A label with no width given measures itself before every paint, not once when it was made | ui.md, ui.lua |
| Aug 2026 | Input interrupts wake a sleeper; they decode nothing. The queue is read in a thread, in its own time | hal/qemu-virt/input.c |
| Aug 2026 | A sleep is against the scheduler's clock, not the counter: only one of them interrupts | syscall.c, thread.c |
| Aug 2026 | "app" is graphical and "program" is console, and they are not interchangeable | glossary.md |
| Aug 2026 | SSH client as M12, with monocypher ported | roadmap.md M12 |
| Aug 2026 | Scripting architecture: every app exposes its hooks as nodes in its own namespace (from BeOS hooks) | beos.md §17.2, roadmap.md M7 |
| Aug 2026 | Name, size and modification date are always indexed; everything else when declared (from BFS) | beos.md §17.2, roadmap.md M8 |
| Aug 2026 | Entity files: nodes with no content, only attributes (from BeOS People) | beos.md §17.2, roadmap.md M7 |
| Aug 2026 | The filesystem is not a relational database. BeOS tried it through DR8 and pulled it for complexity and performance | beos.md §17.1, roadmap.md M8 |
| Aug 2026 | The kernel contains no Lua. Its tests and benchmarks moved to EL0 | state.md, CLAUDE.md |
| Aug 2026 | A monotonic counter is a syscall (SYS_TICKS); the wall clock stays a capability | design.md §4.4, syscall.h |
| Aug 2026 | ramfb before virtio-gpu. `hal_fb_init` is "ask the firmware for a linear framebuffer", which is ramfb and the Pi mailbox both; virtio-gpu comes second and is what grows a `hal_fb_flush` | hal.md, roadmap.md M6 |
| Aug 2026 | The QEMU framebuffer's stride is padded on purpose, so code that assumes `width * 4` breaks here rather than on real hardware | gfx.md §19.3, hal/qemu-virt/fb.c |
| Aug 2026 | A pitch bug is invisible from inside the process: reads and writes stay consistent with each other. The test for it lives outside the guest, in `make screenshot` | gfx.md §19.3, tools/run_screenshot.py |
| Aug 2026 | The screen is a boolean on the process, like the console, and init hands it on. Today to the shell; at the app server, to that instead | state.md, kernel/process.h |
| Aug 2026 | Spleen 8x16 as the bitmap font, BSD-2-Clause, vendored unmodified under `assets/fonts/` and converted at build time. Linux's `font_8x16.c` rejected: it is GPL and would infect the image | gfx.md, assets/fonts/ |
| Aug 2026 | The kernel console can write to the screen. Not a graphics subsystem: forty lines of glyph blitting, so that `panic()` reaches a display on a board with no serial cable | kernel/console.c, CLAUDE.md |
| Aug 2026 | The boot narrates itself in ten numbered stages with a progress bar, because what happens between the reset vector and a prompt is most of what there is to learn here | kernel/boot.h, state.md |
| Aug 2026 | Keyboard input is virtio-input over virtio-**mmio**, not virtio-pci: no bus to enumerate and no capability list to parse, and the same transport gives virtio-gpu next | hal/qemu-virt/keyboard.c |
| Aug 2026 | A keyboard is not a new HAL call. It is another source for `hal_getchar`, so nothing above the HAL changes because one exists | CLAUDE.md, hal/hal.h |
| Aug 2026 | `SYS_SYSINFO` reports raw ID registers and pool counts; the kernel decodes none of it. The tables that name a processor live in Lua | kernel/syscall.h, user/init/init.lua |
| Aug 2026 | Hardware is inventoried at `/dev`, a server reached through the namespace, not a shell built-in | user/init/init.lua |
| Aug 2026 | The shell dispatches bare-word commands before falling through to Lua, and supports aliases. A leading `/` always means a command; a bare word does not when it collides with a Lua name | user/init/init.lua |
| Aug 2026 | CPU usage is sampled at the timer tick as idle-vs-busy, and reported as the difference between two readings | kernel/thread.c |
| Aug 2026 | `def` compiles a line of Lua into a named command; the shell is extensible from inside itself | user/init/init.lua |
| Aug 2026 | The working directory is the shell's idea alone. Servers are always told whole paths | user/init/init.lua |
| Aug 2026 | A listing is the server's entries plus whatever is mounted below the path. Only the namespace knows the second half, which is what makes `/` a directory | user/init/init.lua |
| Aug 2026 | Every fixed pool must be countable and reported. `ADDRSPACE_MAX` was the real limit on processes for a while and no report mentioned it | arch/aarch64/mmu.c, kernel/syscall.h |
| Aug 2026 | Programs live in `/bin`, served read-only from the image, and run in a process of their own with the capabilities the shell chose. No path search, no inherited environment | user/bin/, user/init/init.lua |
| Aug 2026 | A read may span messages (`more`/`offset`) rather than growing `MSG_BYTES`: every thread embeds a message, so raising it is paid for by all of them | user/init/init.lua |
| Aug 2026 | A program can launch a program: the runner hands its child a `run`, so nothing needs to know role numbers and no one can pass on more than they hold | user/init/init.lua |
| Aug 2026 | The project is renamed Kosmos (previously Komo) | all docs |
| Aug 2026 | Pixels never live in Lua tables. A surface is a userdata over flat bytes; tables carry intent | gfx.md §19.1 |
| Aug 2026 | No line of Lua computes a pixel offset. Pitch and format live in the handle, the arithmetic happens only in C | gfx.md §19.3 |
| Aug 2026 | Presentation via double buffering and an explicit `commit` with mandatory damage. No locks | gfx.md §19.4 |
| Aug 2026 | Shared memory mapped inner shareable, no manual `DC` per frame | gfx.md §19.5 |
| Aug 2026 | Surfaces need an explicit `free` plus a `__gc` safety net, and the GC must be told the real size | gfx.md §19.6 |
| Aug 2026 | A shared region maps in its own address window, so no exit path frees pages it does not own | gfx.md §19.8 |
| Aug 2026 | A capability slot carries a kind; one index space for endpoints and memory both | design.md §4.4, state.md |
| Aug 2026 | The context switch saves the whole FP register file; a preempted thread has live values everywhere | state.md, bench/baselines.json |
| Aug 2026 | Face winding is derived from the geometry, never written out by hand | g3d.lua, state.md |
| Aug 2026 | M8: virtio-blk in the kernel HAL, moving to userland when a second driver shapes the interface | roadmap.md M8 |
| Aug 2026 | M8: attribute indices are rebuilt at mount, not stored on disk | design.md §8.3, roadmap.md M8 |
| Aug 2026 | M8: a large file is delivered as mapped pages, never as a string | design.md §8.4 |
| Aug 2026 | The 10k kernel figure is a smoke alarm, not a rule; what the kernel may *contain* is the rule | CLAUDE.md, design.md §2 |
| Aug 2026 | These are principles, not laws: departing from one is a decision to take out loud, never a drift | CLAUDE.md |
| Aug 2026 | Window decoration is one colour across the whole frame; the BeOS title-width tab is departed from | ui.md |
| Aug 2026 | Two palettes, switched live; the palette table is mutated in place so every widget follows | ui.md §16.9 |
| Aug 2026 | A widget colour may be *named*, and a name is resolved on every draw, never captured | ui.md §16.9 |
| Aug 2026 | The microkernel is **Nebula**; Kosmos is the operating system on top of it | CLAUDE.md |
| Aug 2026 | Versions are major.minor.revision: a revision per push, a minor per milestone | VERSION, tools/bump.py |
| Aug 2026 | Action buttons go in a bar across the top of a window's content, never below it | ui.md §16.10 |
| Aug 2026 | A Lua/C crossing costs ~2000 pixels; a primitive writes that much or is batched | gfx.md §19.11 |
| Aug 2026 | Test harness from M0: host-side runner, TAP over serial, exit code via semihosting | testing.md §18.1, roadmap.md M0 |
| Aug 2026 | QEMU with `-icount` for regressions (deterministic), PMU on hardware for budget. They measure different things | testing.md §18.3 |
| Aug 2026 | Anything with a frame budget is measured by max and p99, never by average | testing.md §18.5 |
| Aug 2026 | Every milestone's definition of done becomes a permanent test | testing.md §18.7 |
| Aug 2026 | Outline fonts via `stb_truetype`, vendored unmodified; the bitmap font stays the default | gfx.md §19.12 |
| Aug 2026 | Three font roles - titlebar, text, monospace - because a terminal's must be fixed-width whatever the others are | gfx.md §19.12 |
| Aug 2026 | Fonts are embedded, wallpapers are not: the image is copied per process | gfx.md §19.12 |
| Aug 2026 | A font server is for containing the parser, not for saving memory; shared image pages are what save memory | gfx.md §19.13 |
| Aug 2026 | A grant is asked for only when the machine can give it: a refused spawn is a server that does not start | design.md §17, testing.md |
| Sep 2026 | Kosmos is MIT, and every file we write carries a one-line notice | LICENSE, CLAUDE.md |
| Sep 2026 | Attributes live in one block per file, pointed at by the inode; kind is structural only for directories | kfs.lua, gfx.md |
| Sep 2026 | A cross-machine benchmark measures for fixed time, never fixed work | testing.md §18.10 |
| Sep 2026 | The benchmark engine is a library; `score` prints it and `sysbench` draws it | testing.md §18.10 |
| Sep 2026 | The serialised format is little-endian by decision, not by accident | serialize.c, testing.md §18.11 |
| Sep 2026 | Journalling, ext3-style: journal, commit block, apply, clear. The order is the guarantee | kfs.lua, testing.md §18.12 |
| Sep 2026 | A host Lua, so pure-logic libraries are unit-tested without booting | Makefile, tools/test_kfs.lua |
| Sep 2026 | The language line is structure vs loops-over-bytes, not platform vs application | design.md §6 |
| Sep 2026 | Audio: a virtio-snd driver, WAV first, then a vendored MP3 decoder in userland C | design.md §6, state.md |
| Sep 2026 | Large files cross as `read(fd, buf, n)`: the buffer is a shared region named by a capability | design.md §8.4, layout.md |
| Sep 2026 | A BeOS-shaped layout: `/system` ships, `/user` is installed, `/home` is yours | layout.md |
| Sep 2026 | A finished algorithm goes in C: hot reload is the cost of C, and there is nothing to reload in a codec | CLAUDE.md, design.md §6 |
| Sep 2026 | **Kits**: C libraries reached as `use("/kits/pdf")`, through the namespace like any library | CLAUDE.md, design.md §6 |
| Sep 2026 | Where speed and hot reload disagree, speed wins. What keeps policy servers in Lua is that C buys ~2% there and can overflow a buffer | CLAUDE.md, design.md §10 |
| Sep 2026 | **The look is dimensional on purpose.** Raised, sunken, grooved - a bevel is not decoration, it is a sentence about what a thing does. Reverses "structure not skin" | ui.md §16.8b |
| Sep 2026 | `SYS_CAP_DROP`: a capability can be given back. Sixteen a thread, and nothing released one | design.md §4.3, kernel/ipc.c |
| Sep 2026 | `SYS_SHARE_UNMAP`: releasing a region means losing the mapping, not only the name | kernel/syscall.c |
| Sep 2026 | A PDF is read through a window and never held: the object layer is Lua, the scanner is C | design.md §6, pdf.lua |
| Sep 2026 | FP and SIMD are saved lazily: the switch disarms them, the first instruction that wants them faults | arch/aarch64/fp.c, CLAUDE.md |
| Sep 2026 | The FP trap is armed at both privilege levels, not EL0 alone, because kernel threads have FP state too | arch/aarch64/fp.c |
| Sep 2026 | Strict priority bands with round robin inside each, from QNX. Five named levels, not 256 | sched_prio.c, CLAUDE.md |
| Sep 2026 | A wake preempts a lower band at the next exception, rather than waiting out a quantum | sched.h, thread.c |
| Sep 2026 | The quantum is a variable, because the interesting thing about one is what changes when you change it | sched_prio.c |
| Sep 2026 | Responsiveness is a design goal: Kosmos owes compatibility to nothing and may borrow freely | CLAUDE.md |
| Sep 2026 | The scheduler is swappable while the machine runs; the queues are drained, not dropped | thread.c, scheduler.lua |
| Sep 2026 | Quantum and policy are anyone's to change; priority is not, because bands come from capability | syscall.c |
| Sep 2026 | A shared region is a list of pages, not a contiguous run; the index lives in allocated pages | memobj.h |
| Sep 2026 | A thread holds 32 capabilities, not 16: a graphical application needs more than a shell did | ipc.h |
| Sep 2026 | A full capability table is its own error, not "out of memory" | syscall.h |
| Sep 2026 | A document's own fonts are rasterised by glyph index, cached per face and size, drawn a page per call | docfont.c |
| Sep 2026 | Priority inheritance across IPC: a server runs at the band of whoever is waiting on it | ipc.c, thread.c |
| Sep 2026 | The aim is a fast, responsive graphical OS - BeOS's bet with QNX's answers, targeting a Pi 5 | CLAUDE.md |
| Sep 2026 | A released binary must survive `make stress`; a committed revision need not | CLAUDE.md, Makefile |
| Sep 2026 | Leaks are asserted on `sysinfo` counters, and memory is judged by whether it *stopped* falling | stress.lua |
| Sep 2026 | A server is not promoted by capability; it borrows urgency from its caller and gives it back | process.c |
| Sep 2026 | Audio is in scope; *guaranteed* latency is not. The mixer reports its worst refill rather than promising one | roadmap.md M11a |
| Sep 2026 | Waiting is `sys.sleep`, never `sys.yield` in a loop: yielding is a spin, and two of them cost 89% of the machine to play a tone | syscall.c |
| Sep 2026 | The tick is 250 Hz, chosen by the sound device rather than by convention: a 5.8 ms period cannot be fed on a 10 ms clock, and buffering more does not fix it | kernel.h |
| Sep 2026 | A server waits with `receive`-plus-deadline, never a sleep: a server that sleeps on a timer answers nobody while it sleeps | ipc.c |
| Sep 2026 | **Control by message, data by shared memory.** A stream never travels as a message payload; if it recurs at the hardware's rate, the bytes go in a region | CLAUDE.md |
| Sep 2026 | The language follows the *layer*, not a judgement: C runs on behalf of another process (kernel, drivers, servers, kits), Lua runs for a person (apps, programs) | CLAUDE.md |
| Sep 2026 | A boundary is an agreement: what crosses into the system is a declared struct, not a table. A capability is what you cannot *name*; a struct is what you cannot *say* | audioproto.h |
| Sep 2026 | The namespace is a **kit**, not a server: it is run in the caller's own process, has no endpoint and no thread. This file called it a server for months and an argument was built on the name | glossary.md |
| Sep 2026 | Servers speak `conproto.h` through a **kit** where a protocol has two implementations. `/dev/console` is the only one: a terminal mounts itself as its child's console, so an application answers the same ABI | con_kosmos.c |
| Sep 2026 | **Hot reload removed.** Not outranked - removed. Seven servers went to C and there is no dynamic linking, so nothing is left to reload. ramfs did not have to go and went anyway, at a known price: a milestone's test deleted and §9.1's only live demonstration with it | design.md §10 |
| Sep 2026 | The desktop **carries a drag it never reads**: a `kind` and an opaque string. It is the only process that knows what is under the pointer, and the reply is a one-shot right given to the window that was handed the drop | wm.lua |
| Sep 2026 | **Nothing in the shared image declares storage only one role needs.** There is one userland binary, so ramfs's 2.1 MB store was `.bss` in all sixteen processes. `kosmos_map` at startup instead: 7232 KB a process becomes 5156, and ramfs pays 7308 | ramfs.c |
| Sep 2026 | **The read-only half of the image is mapped where it lies**, one copy for the machine. Permissions live in the mapping, not the page, so sharing 2.8 MB nobody can write costs no isolation. A process holds 2224 KB where it held 7232 | design.md §4.1.1 |
| Sep 2026 | **MMIO is one hand-written instruction**, not a volatile store. GCC chose a post-indexed store and ARM sets ISV=0 for writeback, so no hypervisor can decode it - correct on metal, unbootable under `hvf` | arch/aarch64/mmio.h |
| Sep 2026 | **A query is scoped to the path it was asked at**, not the volume. One disk mounted at three prefixes was answering `/home` with `/system`'s files, and the namespace was putting the prefix on twice | run_queries.py |
| Sep 2026 | **The virtio transport is one file.** It was three copies of a handshake whose *order* is the protocol; the reason not to share it - "before there are two" - expired two devices ago, and the network card is the fourth | hal/qemu-virt/virtio.c |
| Sep 2026 | **A card is a device; a stack is someone you ask.** `/net` is a server behind `SPAWN_NET` - the disk's grant pointed outwards - and the card has no name in any namespace, because nothing but the stack may reach it | user/servers/net.c |
| Sep 2026 | **Ping needs no TCP**, and half a TCP is worse than none. Ethernet, ARP, IPv4 and ICMP is what a round trip costs; `netproto.h` records where the shared ring goes when a *stream* arrives, so nobody discovers a message worked for the first ten kilobytes | netproto.h |
| Sep 2026 | **A connection's bytes never travel in a message.** Two SPSC rings in a region both sides hold, and the receive window *is* the ring's free space - so a client that stops reading really does slow the sender down, and flow control costs nothing | tcpring.h |
| Sep 2026 | **One segment in flight, out-of-order dropped.** A queue and a sliding window buy throughput on a long fat link and buy nothing on a line protocol; a reassembly buffer is the well-known way to get a stack wrong. One timer instead of four | user/servers/net.c |
| Sep 2026 | **A FIN is a wish, not an act.** It takes a sequence number, so sending it while the ring still holds bytes numbers it as if they did not exist - a 152 KB image arrived as 141 KB with the server logging success and the client seeing a clean close | user/servers/net.c |
| Sep 2026 | **A manager is not the server.** `accept` blocks and a window that blocked would stop drawing, so `httpd` is a process and `webserver` reads what it wrote to `/ramfs` - which is why daemons have log files rather than shouting | user/bin/webserver.lua |
| Sep 2026 | **Crypto is checked against its own specification's vectors, in `make test`.** It is the one place here where a bug is silent: a wrong counter still encrypts, and nothing about running tells you | user/lib/crypto.c |
| Sep 2026 | **A coroutine per connection is what this system has instead of threads.** There is no thread syscall and a process has one `lua_State`, so two threads inside it would want a lock around the interpreter and take turns anyway. `httpd` yields where it would have waited | user/bin/httpd.lua |
| Sep 2026 | **Waiting to read and waiting to write are two questions.** `poll` takes two masks. One mask reported writable as "room *and* something queued", so a client that acknowledged the whole ring left the server waiting for a signal that could no longer come - eight requests hung and none were logged | netproto.h |
| Sep 2026 | **Every park in the stack is bounded**, `accept` included. `poll` saying somebody arrived and `accept` reaching the stack are two moments, and a reset in between would wedge an event loop for good | user/servers/net.c |
| Sep 2026 | **A drain loop must not read from the buffer it is filling.** The console's `interrupted` took a byte off the stash, put it back, and took it again - one character typed ahead and the console server span for ever, which looked like whichever program had asked having hung | user/servers/console.c |
| Sep 2026 | **A namespace call behaves the same on every mount, or it is not one.** `fs.write` split long writes for `/ramfs`, and *raised* out of the serialiser on the disk, which takes no offset to append at. It sends a value too big for a message through a region now - the route `write_from` and `files.copy` already used | user/init/init.lua |
| Sep 2026 | **A server lent a buffer gives the capability back, on every path.** diskfs's read side had paid that debt since a PDF found it on its fifteenth read; the write side never had, so it worked for thirty-one writes and then refused every one after with what reads like a bad pointer | user/init/init.lua |
| Sep 2026 | **The allocator scanned every block, allocated ones included**, so what an allocation cost was what the heap already held. Free blocks are binned by size now, with the links inside their own unused payloads: `alloc_table` 28138 -> 356, and `serialize` fell 40% without being touched | runtime/libc/malloc.c |
| Sep 2026 | **A heap that grows is a runtime answer to a runtime question.** 2 MB was fixed at compile time and `make DOOM=1` existed to rebuild the system with a bigger one; `malloc` asks the kernel for another arena instead, to 48 MB. Arenas are never adjacent, so each is its own physical list and coalescing cannot cross | runtime/libc/malloc.c |
| Sep 2026 | **64-bit only, and that is a constraint on word size rather than on architecture.** The Pi 1 was the best board to learn on and is ARMv6; a second `arch/` is expected and wanted, but x86-64 buys everything ARMv6 would have and gives up nothing. 16 AArch64 sites in 8656 lines of kernel say it is not a refactor | docs/hal.md |
| Sep 2026 | **A web page parses on Kosmos.** Hubbub, libdom and libcss running at EL0 on the freestanding libc - three `p` elements counted through a tree walk, `&amp;` decoded from a generated table, a stylesheet understood. Linking proved none of it | tools/run_web.py |
| Sep 2026 | **The kernel has no architecture-specific instruction in it.** Sixteen sites across four files became seven inlines in `arch/aarch64/cpu.h`; the two maskings stayed two, because `DAIFSet #3` is IRQ *and* FIQ where `#2` is IRQ alone, and merging them would silently change when a fast interrupt may arrive | arch/aarch64/cpu.h |
| Sep 2026 | **A browser window, and it says what it is not.** It fetches, parses and shows a document's text, with *text only, there is no layout engine yet* in the status line - the chrome is here early so each piece of engine has somewhere to appear when it lands | user/bin/browser.lua |
| Sep 2026 | **The text path read bytes where it needed characters.** Each byte became a codepoint, so one accented character was three failed lookups and three boxes - never a missing font. Both paths decode now, and glyphs outside ASCII rasterise on demand into a codepoint-keyed cache | user/lib/gfx.c |
| Sep 2026 | **A face is a name and a size, not one of four roles.** A page needs a heading and a paragraph on screen at once; `gfx` held one face per role. Adding weights then broke resolution silently - `-Bold` sorts before `-Regular` and both were called `ibmplexsans`, so the terminal would have gone bold with no error | user/lib/gfx.c |
| Sep 2026 | **A page is painted in one crossing, not one per box.** Layout and drawing both happen in C and what returns is a height; a call per box would cost more than the drawing. Lines break at a measured width, and the ascent that turns a line's top into its baseline stays inside `gfx`, which is the only place that knows it | user/lib/web_paint.c |
| Sep 2026 | **The browser draws its own pixels.** On the ordinary path the compositor rasterises an application's text, in the four faces the desktop chose - right for a dialog, and hopeless for a document wanting a heading at 28 pixels and a paragraph at 16. So it takes a direct window, and pays for it in widgets: the chrome is rectangles it knows the position of, as `pdfview`'s is | user/bin/browser.lua |
| Sep 2026 | **A page is drawn a word at a time, not a line at a time.** Drawing the byte range between the first word and the last is one call instead of a dozen, and it draws the markup's own newlines with it - a glyph nothing has, wider than the space the wrap budgeted, so the line overflowed as well. A word is also where a font change, a link and a selection attach | user/lib/web_paint.c |
| Sep 2026 | **Layout keeps its boxes, and one missing data structure was six missing features.** Painting as the tree was walked threw away where each word ended up: nothing could be clicked, nothing could be bold inside a paragraph, `pre` could not keep its spaces. Layout produces an array of runs; paint is one loop over it and hit-testing is the same loop with a comparison | user/lib/web_paint.c |
| Sep 2026 | **A rectangle is a run with no text.** A heading's rule, a list marker and a link's underline share the array the words are in, so painting is one pass in layout order and a rule cannot land on top of the line it belongs under | user/lib/web_paint.c |
| Sep 2026 | **`-b "wm blocks"` booted `wm` and dropped `blocks`.** The script's own documented example: the boot argument was built as a string and expanded unquoted, so a shell split it and QEMU got a stray word. Every single-word `-b` worked, which is why it survived. A POSIX shell holds a list with `set --`, and a string is not a substitute for one | run-kosmos.sh |
| Sep 2026 | **`/bin` reported 74 of its 82 programs and said nothing.** A listing reply holds 74 names and `BIN_OP_LIST` started at nought every time, so the Deskbar - which builds its menu from that list - could not offer Tracker, the Terminal, the top bar or the web server. The guard was a static assert claiming "a list must hold every program in the image", which is not something a static assert can see | user/servers/binfs.c |
| Sep 2026 | **`make qemu` is the whole system, at 1920x1080.** The variants exist so a suite can be quick, not so the machine you sit in front of is. `VARIANT` had to compose rather than choose - `DOOM=1 WEB=1` called itself `-doom` - and be computed after the block that sets them, because `:=` expands where it stands | Makefile |
| Sep 2026 | **A preference stays chosen; a service is running right now.** The network configuration, the process list, the log and the web server were filed under Applications among the calculator and the paint program. A fourth section, System, which is the one nobody could find the web server without | user/bin/deskbar.lua |
| Sep 2026 | **A browser ships with a page inside it.** Looking at a new build meant starting a web server on the computer running QEMU first, which an operating system has no business asking for - and it took being told so to see it, because the test harness serves pages from the host to stay offline and deterministic, and that got carried into the instructions as a requirement | user/bin/browser.lua |
| Sep 2026 | **`Host` was the literal string `kosmos`.** HTTP/1.0 made the header optional and the web stopped being like that twenty years ago: one address serves hundreds of sites and the header is how a server knows which. Every real host answered the wrong thing, and the only server that looked right was one serving a single site out of a directory - which is what it was tested against | user/bin/browser.lua |
| Sep 2026 | **A released image says whether it has the browser in it.** `-web` in the name, because on an ordinary one the browser opens and reports no web kit - which reads as a broken browser rather than the wrong file. One size rather than three: five vendored libraries take an image from 1.7 MB to 5.3 MB, and stripping saves a quarter of a megabyte because the bulk is the userland compiled in | Makefile |
| Sep 2026 | **A scheduler tick is four milliseconds, and three comments said ten.** `TICK_HZ` went 100 to 250 and `wm.lua` had already stopped assuming. It is not tidiness: every animating window sleeps one tick a frame on purpose, and whether that is a quarter of a frame or most of one is the whole question about what the trade costs | user/lib/sys_user.c |
| Sep 2026 | **Half of a browser frame is waiting, not computing.** Split three ways on this Mac's own cores: 0.1 ms of chrome, 2.8 of blitting the visible band, and 2.8 in a `commit` whose handler swaps an index and records a rectangle. The worst frame moved 10.8-15.6 ms across two runs of identical code, which is scheduling rather than cost - a commit landing mid-pass waits out the compositor's pass | user/bin/browser.lua |
| Sep 2026 | **A space is measured in the face it was written in**, which is neither of the words either side of it. `<code>h1</code> is set` puts the space in the paragraph's own text node, so it is a paragraph-width space; taking it from the following word put a monospace gap in front of every inline code span, and taking it from the preceding one moved that gap to the other side. Found in a screenshot, not in the code | user/lib/web_paint.c |
| Sep 2026 | **`sysinfo` carries `cpu_arch` and an opaque `cpu_raw[]`, not seven registers by name.** The kernel decodes nothing about the processor - the ID registers go out as they were read and what they mean is userland's problem. That division works exactly as long as there is one architecture; with two, "raw" is not enough information, because the same words are MIDR_EL1 on one machine and CPUID leaves on another. Each `arch/*/cpu.h` names its own indices. ARM's decoding did not change | kernel/syscall.h |
| Sep 2026 | **`process_may_read` decoded page descriptor bits, and a search for register names does not find that.** `*entry & 1` and `(*entry >> 6) & 3` - the check on every pointer a process hands the kernel. On x86 bit 6 is Dirty and bit 7 is page-size, so it would not have crashed: it would have answered wrongly about who owns a page, which is the worst way for a security check to be wrong. Now `as_user_may`, answered by each architecture from its own format | kernel/process.c |
| Sep 2026 | **Six places where AArch64 had leaked out of `arch/`, and every one of them is closed.** `kernel/` has never contained a line of assembly, which made it easy to believe it was portable; a second architecture is what asks properly. Seven registers planted by `thread.c`, `enter_el0` declared by hand in `process.c`, `tf->x[N]` ninety-six times in `syscall.c`, the descriptor bits above, the `cpu_info` ABI, and the literal string "MIDR_EL1" in the boot log. All thirteen `kernel/*.c` now compile for x86-64, with no `#ifdef` in any of them | arch/, kernel/ |
| Sep 2026 | **`hlt` is not `wfi`, and the idle loop is where that costs a machine.** AArch64's `wfi` wakes on a pending interrupt with PSTATE.I set - masking stops the exception being *taken*, not the wakeup - so the idle loop masks across "is anything runnable" and "sleep" to close the race between them. `hlt` with IF clear halts and nothing wakes it. The same loop, unchanged, gave twenty timer interrupts in the first fifth of a second and then a machine that had printed its prompt and stopped. `sti; hlt`, whose one-instruction interrupt shadow exists for exactly this | arch/x86_64/cpu.h |
| Sep 2026 | **A syscall preserves every register but three, and `syscall` saves none of them.** AArch64's `svc` is an exception like any other: the vector writes all thirty-one general registers into the trapframe and `eret` reads them back. `syscall` writes rcx and r11 and leaves the rest as the process had them, so the entry stub must save what the kernel's C is about to clobber. Getting it wrong is not a crash but a caller whose live variable changed under it - here, `lua_pushinteger(L, sys0(...))` with `L` quite correctly kept in rdi | arch/x86_64/user.S |
| Sep 2026 | **The kernel's boot log printed string literals about ARM hardware.** "PL011 UART at 0x09000000" and "250 Hz off the generic timer through a GICv3", on a PC that has neither. The same mistake as decoding a page descriptor in `process.c`, one layer up and in prose. The *why* of a boot stage is about the design and stays in the kernel; what this machine turned out to be is the board's to say, and the architecture's for the two in `arch/` | hal/hal.h |
| Sep 2026 | **Finding a device and claiming it are two steps, and they were one.** `virtio_open` reset what it found, which is fine for a driver that wants the first card of its kind and wrong for `input.c`: a keyboard and a tablet are both virtio-input, and telling them apart means opening each. The tablet's scan reset the running keyboard - no queues, no DRIVER_OK, silent for ever. It worked on ARM by luck, QEMU laying mmio windows out in reverse so the tablet came first; swapping two `-device` flags would have broken it there too. `virtio_begin` is now separate | hal/virtio/virtio.h |
| Sep 2026 | **A declaration left behind in a board's header is invisible to the second board.** `keyboard_getchar` moved into the shared `hal/virtio/input.c` and its declaration stayed in `hal/qemu-virt/qemu-virt.h`, so `hal/pc/uart.c`'s `hal_getchar` read only the 16550. The PC found a keyboard, said so in the boot log, and could not be typed at: every key went into a virtqueue nothing drained. A declaration belongs with the code that answers it, not with the first board that happened to call it | hal/virtio/virtio.h |
| Sep 2026 | **The x86 build had no screen size, so it used the fallback.** `FB_FLAGS` is on one file's compile line on ARM, because putting it in CFLAGS rebuilt seventy-eight objects to change a number seven of them have never heard of. The x86 kernel is a single compile-and-link with no objects to be finer than, and the flag was simply never added - so `ramfb.c` took its 1024x768 default and the machine came up at a resolution nothing had asked for. It reads as a display bug and is a Makefile line | Makefile |
| Sep 2026 | **A harness nothing ran had been failing for months, and took ten minutes to say so.** `run_disk.py` was reachable only through `disktest`, never `make test`, so nothing noticed when `mounted()` started formatting a blank disk on its own: the harness still insisted a zeroed disk report no filesystem, which by then it never would. The other half was self-inflicted - `each` was a *duration*, so every command waited twenty-five seconds whether the shell answered instantly or not at all. It is a deadline now, matched against the prompt the boot already waits on: 26 checks in 4.7s rather than ten minutes, on both boards | tools/run_disk.py |
| Sep 2026 | **The clipboard went behind the window manager's prefix, because Control-C is spoken for.** There are no modifier keys here - a virtio keyboard gives Control plus a letter, and Control-C is what stops a program, in nine display checks. The alternative was inventing a triple out of unclaimed control codes, where every candidate carries somebody's prior. `Control-W a c x v` instead: behind a prefix the letters can be the ones everybody knows, which is what a prefix is for. The window manager holds the buffer and is told the *intent* rather than the text, so it never looks inside - the same division it keeps with pixels **Reversed in September 2026, two rows below: a modifier arrived and Control-C turned out not to be spoken for after all.** | ui.md §16.11 |
| Sep 2026 | **A cap enforced in the server would be too late by one process.** A clipboard entry travels inside a message and `MSG_BYTES` is 2048, so a copy of a four-kilobyte report cannot fit - and a table too big to serialise makes `fs.send` *raise in the caller*, killing the application that copied it. So `wmproto.copy` cuts before sending, and `ui.editor` shrinks the highlight back to the run that actually left: the limit becomes something you can see, in the place you were already looking | user/lib/wmproto.lua |
| Sep 2026 | **On x86-64 you cannot step past a faulting instruction, so the suite uses the recovery that works on both.** AArch64's handler does `elr += 4` and the arithmetic is exact, every A64 instruction being four bytes; an x86 instruction is one to fifteen bytes and its length is unknowable without decoding it. The *unwind* form - which ARM already had, for the stack-overflow case where stepping would resume into the guard page - is what both use now, behind one `FAULT_EXPECT` macro. It cost ARM nothing and it is what let `tests.c` cross to a second board | tests/fault.h |
| Sep 2026 | **A test that writes out an address is a claim about one machine.** `as: the kernel region is refused` named 0x40000000, which is a kernel address on AArch64 and is *exactly* `USER_VA_BASE` on x86-64 - so on the second board it asked the kernel to refuse the first page of user space, the kernel correctly mapped it, and the test reported a failure entirely its own. Derived from `USER_VA_BASE` now | tests/tests.c |
| Sep 2026 | **`arch/x86_64/fp.c` said preemption was not safe, and it had been safe for as long as `switch.S` had two instructions in it.** The comment claimed the FP mechanism was not built, that the 512-byte area was not in `struct context`, and that "nothing here preempts yet" - all false, and the last dangerously so: the timer preempts on this board like any other, and the eager `fxsave`/`fxrstor` is exactly what makes it correct. Written before those instructions existed and never revisited | arch/x86_64/fp.c |
| Sep 2026 | **Every x86-64 vector had `ist = 0`, so a kernel stack overflow triple-faulted the machine.** A fault from ring 0 stays on the stack that faulted, which is fine for a deliberate one and fatal for an overflow: the handler pushes onto the guard page and faults again delivering that. AArch64 gets the separation from the architecture - the kernel runs on SP_EL0 and every exception switches to SP_EL1. Fixed with a 16 KB exception stack, its own guard page, and #PF and #DF pointed at `tss.ist[0]`. **Two vectors and not all of them**, because an IST stack is not reentrant: the processor loads the same address every time, so a fault taken while one is being handled overwrites the frame being handled | arch/x86_64/trap.c |
| Sep 2026 | **The block driver ignored interrupts, which is a choice on one bus and a defect on the other.** A virtio-blk request completed correctly on x86-64 - QEMU's trace says `virtio_blk_req_complete status 0` - and the guest then made no progress at all. Not the spin loop failing to see the used ring: **a PCI INTx line is level-triggered and shared**, a device holds it asserted until its own ISR byte is read, and `hal_irq_handle` offered the line to input, sound and net and to nothing else. The PIC was told the interrupt was handled, the line was still asserted, and it fired again immediately, forever. AArch64 never had it because every device there has an interrupt ID of its own and blk never enables its own - sharing is what turns "this driver ignores interrupts" into a bug | hal/virtio/blk.c |
| Sep 2026 | **A field called `ticks` meant scheduler ticks in three of the four operations that read it and counter ticks in the fourth.** `NET_OP_RESOLVE` added a caller's deadline to `kosmos_ticks()` without converting, so five seconds was twenty microseconds and **the resolver answered whichever query beat the next sweep and timed out the rest** - it had never worked twice in one boot. The browser was its only caller and passed `counter_hz`, wrong in the same direction by the same factor, so the two agreed and nothing noticed. This is 0.9.1's bug in another subsystem, and it has the same fix: the unit goes in the name, and the conversion is one function | user/include/netproto.h |
| Sep 2026 | **A resolver with no command is a resolver nobody can test.** DNS was complete in `/net` - query, reply, label lengths, compression pointers - with one caller four hundred lines inside a graphical application. So a lookup could not be tried alone, a bad name could not be told from a bad route, and `make test` had nothing to check; meanwhile five separate comments, two programs and the roadmap all said the system had no DNS. `host` is thirty lines and found a bug the same afternoon | user/bin/host.lua |
| Sep 2026 | **A deadline is a counter value, not a count of interrupts.** `wake_at` was `hal_ticks()` plus a number of scheduler ticks, and `hal_ticks` counts interrupts *actually taken* - `hal_ticks_missed` exists because they are not - so a machine under load ran every sleep long by however many it had missed. A clock that stretches exactly when something is already going wrong. It also cannot be handed to a comparator, so the periodic tick could never have been removed while deadlines were expressed in it. One conversion now, in `thread_deadline_in`, and `architecture.md` §5 is the account of all three clocks | kernel/thread.c |
| Sep 2026 | **A deadline is not a reason, and on x86-64 every timeout in the system was four milliseconds.** `thread_wake_sleepers_now` woke every thread carrying a `wake_at`, on the grounds that a deadline meant waiting - but a `sys.sleep` carries one and waits for nothing. `input_arrived` is cleared only by `SYS_WAIT_INPUT`, which nothing calls at a bare prompt, so on that board the flag latched and every timer tick ran the path: 1200 times in five seconds against 0 on AArch64, with identical code. `ping`'s one-second gap between echoes took 40 ms. Found by timing a sleep from outside the guest, because **nothing in the suite had ever checked that a sleep lasts** | kernel/thread.c |
| Sep 2026 | **Drivers group by what the device is, not by architecture and not by board.** The obvious grid - arch down one side, board along the other - breaks on a specific pair: QEMU's q35 and an Alienware x14 share an instruction set and *almost no device*, while that laptop and a Pi 5 share no instruction set and the entire USB stack. So three axes: `arch/` for the ISA, `hal/<platform>/` for **how a machine says what it has**, and a pool of drivers matched at run time. `hal/virtio/` and `hal_bus_scan` are already both | docs/targets.md |
| Sep 2026 | **A portable file explaining itself in one architecture's vocabulary hides whether the *claim* is portable or only the code is.** `kernel/`'s thirteen files compile for both boards with no `#ifdef` and their comments still said EL0, SP_EL1, TTBR0, CNTFRQ_EL0 and `svc`. Two were not merely parochial but wrong: `USER_TEXT_VA` and friends carried absolute addresses that are false on x86-64, and `process.c` justified a shared code mapping as an ARM rule about memory attributes when the requirement is general and x86 spells it with the PAT. The twelve references left name both boards on purpose | kernel/process.c |
| Sep 2026 | **`TPIDR_EL1` is banked, so the per-CPU pointer costs one store at boot and touches no entry path at all.** `smp.md` had said of it and x86's `GS` base that "neither is written... every exception entry has to establish it", which is true of x86 - one `GS` shared between ring 3 and ring 0 means `swapgs` at every boundary - and wrong about AArch64, where EL0 can neither see nor change the register. So SMP step one cost one line at the top of `kmain` rather than a pass over the vectors, and this is now the strongest reason to do SMP on ARM first | kernel/percpu.h |
| Sep 2026 | **Build the instrument before the experiment, and it finds bugs in itself.** `cores` shows a bar per processor and buttons that add a compute-bound worker - one bar today, and the point is that a second one is what makes a second core visible. Writing it found that the loop filling `sysinfo.cpu[]` was bounded by a field assigned seventy lines further down, so every core read 0% with two spinners running. Nothing else would have noticed: the *total* was right, and the total is what the other four readers use | user/bin/cores.lua |
| Sep 2026 | **PSCI has no "how many processors" call, so ask about each one until the firmware refuses.** `AFFINITY_INFO` is a query about a *specific* processor and answers `INVALID_PARAMETERS` for one that does not exist - so counting up to the first refusal is the count, with no device-tree parser and nothing started. `make test` boots `-smp 4` now, because on one core a working discovery is indistinguishable from a hardcoded 1 | hal/qemu-virt/power.c |
| Sep 2026 | **A processor count is the wrong question on every machine after QEMU.** Alder Lake and big.LITTLE have cores that differ three ways - kind, whether two threads are siblings on one core, and which cache they share - and "twenty threads" describes none of it. Not built, and the point is narrower: **the runqueue split at SMP step 5 is the decision that would be expensive to get wrong**, because a central queue that cores pull from cannot express "this belongs on a P-core" and retrofitting that is a rewrite | docs/smp.md |
| Sep 2026 | **A test that knows only the calm colour fails exactly when the machine is busiest.** The meter check looked for `theme.good` and found none, because one spinner on one core settles at 100% and the bar had already turned `theme.bad`. And the first version left the worker running - `spin 600`, detached, so Control-C to the window manager does not touch it - and the *next* phase's window never appeared, with the harness blaming the window rather than the spinner behind it | tools/run_screenshot.py |
| Sep 2026 | **Auditing a plan against the code found a seventh thing the plan had missed.** `smp.md` listed six globals that must become per-CPU; `preempt_pending` is a seventh - a `volatile bool` saying a switch is owed on *this core's* way out of an exception. Found by reading `thread.c` for the other six, which is the case for auditing against code rather than against prose | kernel/thread.c |
| Sep 2026 | **On a PC there is no board, there is a bus.** Embedded machines do not enumerate - the Pi 5's UART is at an address you are told - so a board file is the right unit there. A PC enumerates itself through ACPI, PCI and USB, so a PC target is a discovery *mechanism* plus a driver pool; `hal/alienware-x14.c` would be wrong for the next laptop and wrong for this one after a firmware update. Which also makes `hal/pc/` an honest misnomer: it is QEMU's q35, and `power.c` says so about an address it hardcodes | docs/targets.md |
| Sep 2026 | **Locks nothing contends are untestable, so the second core came before them.** `smp.md` step two is "take locks with one core"; both boards already enter the kernel with interrupts masked, so on one core no pool needs one - every lock added would be a large diff that nothing contends, nothing fails on and `make test` cannot tell correct from broken. Step three has the opposite property: a parked core touches no shared structure, and it is the only way to check that step one's per-CPU register is genuinely per-core rather than merely asserted on a machine where every answer is the same answer | kernel/smp.c |
| Sep 2026 | **Both bugs in starting a second processor were ordering, and the loud one was silent.** `smp_start_others` ran fifty lines before `mmu_init`, so secondaries enabled translation with tables that did not exist - no fault, no hang, just a boot line saying 1 where it should have said 4, which is the line least likely to be believed on the day it is written. The other: `thread_cpu_count` returned `NR_CPUS`, claiming four scheduling cores with three in `wfi`. `NR_CPUS` is how many slots exist; how many schedule is a different number and is still one | kernel/main.c |
| Sep 2026 | **A link error on the other board is what put the MMU enable in the right file.** `secondary_main` began with `mmu_enable_here()` - an AArch64 function called from `kernel/` - and x86-64 refused to link the moment `kernel/smp.c` joined its build. How a started core reaches a state where it can execute C is what `arch/` and `boot/` are for, and the boards disagree: one arrives with the MMU off, the other will arrive from a real-mode trampoline already in long mode. So `boot/start.S` calls it and `kernel/smp.c` has no architecture in it | boot/start.S |
| Sep 2026 | **The board knows how to start a processor and not where it should land.** `hal_cpu_on` sends PSCI or `INIT`-`SIPI`-`SIPI`; `cpu_secondary_entry` in `arch/` says what a started core runs, and answers 0 on x86-64 where no trampoline exists. Two refusals rather than one - no landing pad in `arch/x86_64/cpu.h`, no local APIC in `hal/pc/cpu_on.c` - kept separate so building one does not silently look like building both | arch/x86_64/cpu.h |
| Sep 2026 | **`make qemu` boots four processors, not only `make test`.** `SMP ?= 4`, with `make SMP=1 qemu` for the one-core machine. The argument is the silent bug above: a bring-up path visible only in a boot line should run every time somebody boots the thing, not only when the suite does. Three cores in `wfi` cost nothing - a QEMU thread that is never scheduled | Makefile |
| Sep 2026 | **A test that names a cause is worth less than nothing when the harness is the cause.** The network suite waited for a prompt after each command and never did: `wait_for` searches the whole buffer, `seen` was cleared once before the loop, so a prompt from an earlier command satisfied every later wait instantly. It passed nearly always - until the second `host example.com`, the one command with a real round trip behind it, lost the race and the check confidently blamed a units bug fixed days earlier. `run_disk.py` had solved this months ago with a `since` offset and a one-line comment; nothing connects two harnesses written months apart | tools/run_network.py |
| Sep 2026 | **Every graphical application's crash has been silent, for as long as there have been graphical applications.** `run`'s detached path was `pcall(chunk)` with the result unread, and the window manager launches every application detached. A second silence compounded it: a window whose process had died stayed on screen for ever, fully drawn, because the compositor owns the pixels. Together they make a dead application and a busy one *identical* from outside - and an afternoon went into a window that had died, read first as a hang, then as a lost mouse event, then as a message-size limit | user/init/init.lua |
| Sep 2026 | **The freeze did not exist, and the diagnostics found that in three runs.** With a detached program's error reported, the app turned out to receive the press *and* the release, fire `on_click`, and start its worker - it was the *test* that was wrong. `check_cores` slept exactly five seconds and took one reading, and a segmented bar climbs a segment at a time where a solid fill crossed the threshold instantly. The same fixed sleep was on the way down too, and fixing only the way up left it red one run later: one property, two places | tools/run_screenshot.py |
| Sep 2026 | **A key is an event and a position is a state.** The console filled the pointer into its `wait` reply *before* sleeping, so every sample the window manager got was one wake stale. Keys queue and lose nothing by arriving a pass late; `kosmos_pointer` answers where it is now, and the manager samples once a pass | user/servers/console.c |
| Sep 2026 | **A test that consumes a whole fixed pool is an undeclared assertion about the entire machine.** `as: one space per possible process` took all thirty-two address spaces, so it passed only while nothing anywhere held one - two assertions in a trenchcoat, and only one of them written down. It went red in two runs of three after the suite started booting `-smp 4`, not for want of memory (four thousandfold margin) but because another test's process had not finished leaving. It asks the pool its size now, and takes only what is free | tests/tests.c |
| Sep 2026 | **`volatile` is not a barrier.** A secondary filled its `struct percpu` and then incremented a `volatile unsigned online` that core 0 spun on; `volatile` stops the compiler caching the variable and says nothing about the order two stores become visible in. On a weakly ordered machine core 0 was architecturally allowed to see the count rise before the slot was written. `cpu_publish` is `dmb ishst` and `cpu_observe` is `dmb ishld` - named, paired, and not `dsb sy` | kernel/smp.c |
| Sep 2026 | **Ten build directories at the top level, and `make clean` named four of them.** `build-user$(VARIANT)` sat beside `arch/` and `kernel/` in every listing, at 802 MB, and the six it did not name are how they accumulated. The Makefile said a distinct top-level directory was necessary because `build/user/x.c.o` would also match the kernel's `build/%.c.o`; it does not, because an object keeps its source's path, so the kernel's pattern would need a prerequisite that does not exist and make discards such a rule. Everything generated is under `build/` now | Makefile |
| Sep 2026 | **One hundred and sixty-six files did not carry the licence line the first rule in this file demands.** Whole directories of them - every one of `arch/aarch64/`, most of `kernel/` and `user/lib/`. A rule that is not checked is a rule that is followed by whoever remembers it, and the shebang is why a naive check under-counted: in a Python file the notice goes on line two | arch/aarch64/cpu.h |
| Sep 2026 | **A processor's interrupt controller and its clock are not the machine's.** `VBAR_EL1` is banked, the GIC redistributor is one per core, and CNTP_CVAL/CNTP_CTL are banked - all three were written as though they belonged to the computer, so a secondary ran with a vector table it never set, configured core zero's redistributor, and shared one comparator with three other cores. Found the redistributor by walking GICR_TYPER and matching MPIDR rather than indexing by a stride, because the architecture has each one declare its own affinity | hal/qemu-virt/gic.c |
| Sep 2026 | **Every lock in this kernel masks interrupts, and there is no second flavour.** The structures worth locking are reached from both a syscall and an interrupt handler - the runqueue from `thread_yield` and from `thread_tick` - so a core holding one with interrupts on can be interrupted into code wanting the same lock, on the same core. That is a self-deadlock no amount of spinning resolves, and it is silent. Masking on this core is enough; a different core spins and gets it | kernel/spinlock.h |
| Sep 2026 | **A lock is the one thing in a kernel that cannot be tested by using it.** Every structure it protects works perfectly with a lock that does nothing, on a machine where nothing contends - which is why step two called an uncontended lock untestable and skipped it. So the test checks the mechanism rather than the protection: it excludes, it names its holder, and it masks. Both failure modes were confirmed by breaking it | tests/tests.c |
| Sep 2026 | **Every pool scanned for a free slot, built in it, and marked it taken last.** The window between finding and claiming is hundreds of instructions - page allocation, a 512-entry table copy - so two cores in `SYS_SPAWN` get the same thread, process and address space with nothing noticing. Moving the claim inside the lock was not enough: `process_create` then ran `memset(p, 0, sizeof *p)` over the slot, clearing the flag that claims it. The suite caught that on the first run | kernel/process.c |
| Sep 2026 | **The IPC critical section has to span the block, not end at it.** `ipc_call` wakes the receiver and only then records `waiting_on` and blocks, so on two cores the peer can reply before the sender has blocked and `ipc_reply` reads a NULL `waiting_on`. That does not corrupt memory - it silently loses a message. One lock per endpoint, held across `thread_block` and released on the far side of the switch; no operation in `ipc.c` touches two endpoints, so per-endpoint costs nothing | docs/smp.md |
| Sep 2026 | **A thread has a home core and does not migrate.** `t->sched.cpu`, assigned round robin at creation, with one runqueue per processor and the vtable naming the queue - `enqueue(cpu, t)`. The alternative, one queue every core pulls from, looks simpler and cannot be undone: it makes every scheduling decision a contended write to one list, and it has nowhere to say "this belongs on a performance core". Work stealing is deliberately absent, because it would need an order between two cores' queue locks and nothing has measured a need | kernel/thread.h |
| Sep 2026 | **The placement decision paid for itself in the hardest place.** The audit said IPC's section had to extend past `thread_block`, handed to the next thread and released beyond `context_switch` the way Linux releases `rq->lock` - because a thread marked blocked and findable can be woken by a second core and resumed by a third on the stack the first is still saving. With a fixed home that cannot happen: a wake only enqueues on the thread's own core, and only that core picks, and that core is the one inside the switch. So the release happens before the switch and the hand-off machinery is unnecessary | kernel/thread.c |
| Sep 2026 | **`sched_switch_to` refuses once more than one processor schedules, and that is a feature lost rather than a detail.** Swapping the policy means draining every runnable thread from one into the other, which with a queue per core is every core's queue at once - the single operation that would define a global lock order, requiring the other cores to be stopped mid-decision. It exists to show mechanism and policy are separable, which it has done; it is not needed while four cores are working | kernel/thread.c |
| Sep 2026 | **The console lock is taken around a whole string, not a character.** The state would be safe either way; the output would not - two cores printing a line each interleave into two unreadable ones. Which is also the hazard it created: `spin_panic` prints from whichever core deadlocked, so if the lock that deadlocked *is* the console's, reporting it through `kputs` spins on the same lock and recurses until the stack runs out. It writes to the UART directly now | kernel/spinlock.c |
| Sep 2026 | **The IPI handler is empty, and that is the design.** The sender has already put the thread on the target's runqueue; the only thing missing is for that core to *look*. Taking the interrupt is what gets it out of `wfi` and into the exception epilogue, where the scheduler already runs. An IPI with a payload would be a message, and messages between processors are what a runqueue and a lock already are | hal/qemu-virt/gic.c |
| Sep 2026 | **The IPI is worth 25x, and the number matches the reasoning exactly.** A cross-core wake takes 11,688 counter ticks with it and 293,688 without - and 293,688 is 4.7 ms, which is one scheduler tick at 250 Hz, precisely what was predicted from reading the code. Nothing depends on it for correctness: the check passes with it removed, because the wake is late rather than lost. So its value is a measurement and not an assertion | docs/smp.md |
| Sep 2026 | **"Every thread is blocked" was a statement about the machine and is now about one processor.** With a queue per core an empty runqueue is the ordinary state of an idle processor, not a deadlock - so `thread_block` falls back to that core's idle thread, which is what an idle thread is for. Found by turning placement on deliberately: three cores reached the panic within a second of getting their first thread | kernel/thread.c |
| Sep 2026 | **A position crosses the boundary in frames, and it is the one number here that needs no conversion.** `frames_played` in `struct audio_ring` counts the frames of a stream that have left the speaker, written by the server into the client's own ring. The two-clocks rule exists because a number mailed to another process arrives naked, and it has cost this system two silent bugs; a frame count cannot arrive naked, because the unit is in its name and there is nothing to divide by. Latency becomes `write * period_frames - played`, which is the ring and the device's queue together without either side having to know how the total is split between them | user/include/audioring.h |
| Sep 2026 | **The device queue is why the position cannot be computed by the client.** A client knows what it wrote and the ring says what was taken, but between "taken" and "heard" sit up to `device_depth` periods that only the audio server can see. A position built from the ring alone is ahead by about 23 ms - small enough to look correct and large enough to be useless for a progress bar or for A/V sync, which are the two things a position is for | user/servers/audio.c |
| Sep 2026 | **The ring's depth is the client's choice, because a shallow ring and a deep one are both right.** A game wants the sound to arrive with the frame that caused it and would rather risk a gap; a video player wants a clock to hang pictures on and would far rather be 200 ms behind than skip, because a gap desynchronises everything after it where latency is a constant you schedule against. One constant cannot serve both, so `audio.open` takes a depth and `audiolag [periods]` is how the trade gets measured rather than argued | user/lib/audio.lua |
 user/lib/audio.lua |
| Sep 2026 | **A dozen tests failed when placement was turned on, and they were right to.** They are single-core tests of the mechanism - they mask interrupts, create three threads and drive them by yielding, which only works if those threads are on this core. Rewriting them to tolerate placement would be rewriting them to stop asking their original question, which is why `thread_create_on` exists instead of a global switch | tests/tests.c |
| Sep 2026 | **A console write carries a colour, and the colour is a field rather than a mode or an escape code.** The console has several writers - the server serves every program, a Terminal serves its children - so set-colour-then-write is two operations another process can get between, and one program's colour lands on another's line. Attached to the bytes it applies to, that cannot happen. And in-band escapes were already refused here once, for the boot log, on the grounds that a server should not have to parse its payload to find out what the payload means | user/include/conproto.h |
| Sep 2026 | **A line in several colours is several writes, not a list of runs on the wire.** `emit` on the far side appends until a newline arrives, so the pieces join up on their own - which means the protocol can already say everything a run list would say, and a run list would be a second way to say it. The banner's wordmark, which has stripes either side of it and through it, goes out as five or six writes and arrives as one line | user/bin/neofetch.lua |
| Sep 2026 | **The kernel's console decodes UTF-8, and the font carries the Block Elements.** It took a byte and drew it, so a three-byte character arrived as three hollow boxes on screen while the serial line - whose far end decodes for itself - showed it perfectly. The same output looking like two different outputs is the complaint the early-run colour list was written to answer, in the other direction. `bdf2c.py` emits U+2580 to U+259F whole, because "which of the blocks" is a judgement nobody should have to make twice | kernel/console.c |
| Sep 2026 | **Colours resolve to ARGB before the message leaves the program.** A console program has no theme - it never loads `ui.lua`, and the console it writes to may be the kernel's, which exists before the desktop and has no palette to consult. So `ns.write` maps `good` and `bad` to numbers on the way out, and what it gives up is output that follows a theme change. The field is already the right width to carry a name instead, so that decision can move without the protocol moving | user/init/init.lua |
| Sep 2026 | **A console write was giving a capability away, and the length of the text chose which one.** `con.encode_request` returned the bytes *and* how many were taken, and Lua expands a call in final argument position to all of its return values - so `sys.call_raw(cap, con.encode_request{...})` was really passing that length as `pass`, the capability to send with the message. A three-character write handed over capability 3. An ordinary line is longer than any index a writer holds, so the transfer failed harmlessly and nothing looked wrong; it took a banner drawn in short coloured runs to make every write a valid index, at which point a terminal ran out of capability slots and could no longer start a program. **The unused return value was the whole bug**, and it is gone | user/lib/con_kosmos.c |
| Sep 2026 | **A terminal absorbs a burst of writes and paints once.** Its drain was non-blocking, so after answering a write it asked again before the child it had just woken could have been scheduled - found nothing, returned, and repainted. One write, one full repaint. A repaint ships the whole window as commands in 1200-byte batches, so `n` writes answered with `n` repaints is `n^2 / 11` round trips: 24.0 seconds for a twenty-three-line banner, measured, with the processor idle throughout. `receive_raw` already took a timeout and this was not using it. The burst is bounded in time rather than in messages - one frame, because one frame is all a screen could have shown | user/bin/terminal.lua |
| Sep 2026 | **`/data` is `/ramfs`, because the root is a list of services rather than a filesystem.** Every other mount is named for its role - `/bin`, `/lib`, `/dev`, `/app`, `/net` - and `/data` was the only one whose name said nothing, while implying the one thing that was false: data is what you least want to lose, and this mount is gone on reboot. A Unix-shaped name also makes the top level read as directories on one filesystem, which is the wrong model for a root made of mounts from five different servers. `/ram` was rejected for reading like a device you write bytes to; the medium is not an implementation detail here, it is the only user-visible fact that distinguishes the mount | user/servers/ramfs.c |
| Sep 2026 | **The mount now has `mkdir`, `delete` and `rename`, which it never had.** Everything that had ever used it *published* - a replicant writing its own source, the web server its status, a benchmark tagging files to query for - and nothing took anything back out, so an operation with no caller was never written. What turned that from an absence into a bug was `rm`: a verb that works on one mount and not another is the namespace failing at the one thing it exists for. `rename` carries its destination in the union rather than a field of its own, because 128 bytes in the struct would be copied on every list and read that never looks at them | user/include/ramproto.h |
| Sep 2026 | **A board binds the HAL; a driver does not define it.** `hal/virtio/snd.c` was the sound HAL, which was exactly right while both boards took sound from it, and stopped being right when the PC had a choice - an Intel HDA controller when there is one, virtio when there is not. Two drivers cannot define the same eight symbols. So the driver exports its own names and a few lines per board map them, which is what `input_bind.c` already did for a keyboard and a pointer that come from different chips. Each `describe` exists for the reason the boot log names the framebuffer's source: a board with two possible answers has more than one way to fail, and on a machine with no serial port they all look like silence | hal/pc/snd_bind.c |
| Sep 2026 | **A sound controller is found by its class, not by its identifier.** `pci_find` matched a vendor and a device id, which is right for virtio - being virtio is what a virtio device *is* - and wrong here: QEMU's HDA controller answers 8086:2668 and a Comet Lake's answers something else, so either id would work on exactly one machine. `pci_find_class` shares the same bus walk rather than copying it | hal/pc/pci.c |
| Sep 2026 | **The HDA driver derives its depth from the hardware's read pointer and never from a counter of its own.** A virtio sound device is a queue; HDA is a cyclic buffer that never stops, so there is no handing over and no running out - only being late. `SDLPIB` says where the controller has read to, one slot is always left free so that write-equals-play means empty rather than full, and a finished period is zeroed in the interrupt handler - because a ring that runs dry repeats the last thing anybody wrote at 172 Hz, where a queue would simply go quiet | hal/pc/hda.c |
| Sep 2026 | **`RINTCNT` is not only an interrupt threshold, so the response interrupt is enabled even though every verb is polled.** It is also how many responses the controller writes before it treats the response ring as full and stops consuming commands, and what restarts it is the driver acknowledging `RIRBSTS`. With the interrupt disabled there is no status to acknowledge, so the controller answers exactly one verb and then nothing, for ever: `GET_PARAMETER` on the root node was correct and the same call one node down timed out. `INTCTL.CIE` stays clear, so the status is set, every response clears it, and the pin never moves | hal/pc/hda.c |
| Sep 2026 | **`make x86` gives the machine an HDA controller instead of a virtio sound device.** The argument the i8042 already won: the driver that has to work on a laptop should be the one exercised every time somebody runs the system, and a machine configured with the easy device leaves the real one untested. `virt` still runs virtio-sound, so neither is orphaned - and the harness reads the audio back off QEMU's `wav` backend rather than trusting the boot log, because a driver that programs the controller wrongly prints exactly what a working one prints | tools/run_x86.py |
| Sep 2026 | **The boot page tables map four gigabytes, not one.** A multiboot loader hands over a 32-bit pointer, so its information structure may be anywhere below the four-gigabyte line; QEMU with 4 GB put it at 0x7ffe2349 and the first read of it faulted at boot stage three, before the framebuffer exists and on a machine with no serial port. Four page directories, 16 KB of `.bss`, and 2 MB pages rather than the optional 1 GB ones | boot/x86_64/start.S |
| Sep 2026 | **A PC's largest usable memory region is not the one to use; the largest *reachable* one is.** With four gigabytes or more the PCI hole splits RAM, so the biggest block sits above the four-gigabyte line - the one the kernel is not loaded into and cannot map. `pmm_init: the kernel image does not fit in RAM` was exactly right. Each region is clipped to what the address space can describe before it competes on size | hal/pc/memory.c |
| Sep 2026 | **Too much memory caps rather than panicking, and says what it gave up.** The old comment argued a machine with more RAM than the kernel can describe should refuse to boot rather than run on part of it - right when the alternative was a silent mistake, wrong now that the alternative is not booting. The panic reached a serial port and the machine this is aimed at has none. It runs on the 766 MB it can map, prints the 16383 MB it cannot, and `mmu.h`'s high-half split is what lifts the ceiling for good | hal/pc/memory.c |
| Sep 2026 | **The kernel's fine-grained map starts at the image, not at the RAM base.** They are the same address under `-kernel` and are not under UEFI, where the firmware's own allocations push the largest low region to 0x900000 while the image sits at 0x100000 outside it. The subtraction wrapped and the map asked for four quadrillion pages. The lower of the two is right either way | arch/x86_64/mmu.c |
| Sep 2026 | **A loader's framebuffer has to be mapped; ramfb's did not.** ramfb's pixels are memory the guest allocated, so a raw pointer works and the missing line was invisible. A firmware framebuffer is at 0x80000000: the first pixel faulted, the fault handler tried to report it, and the console lock was already held by the write that faulted - `spinlock: console held by 0, wanted by 0`, for ever, which on a laptop is a black screen that cannot even reach a serial port | hal/pc/fb.c |
| Sep 2026 | **`make x86-iso` builds a stick, and the suite boots it.** QEMU's `-kernel` is not a loader: it does not answer the multiboot video request, so the path a laptop depends on entirely had never run while fourteen host checks on the decision all passed. `run_uefi.py` boots the real image under OVMF and checks the *screen*, because a machine whose framebuffer works stops talking to the serial line at stage six. GRUB is told `insmod efi_gop`, since `all_video` picks its own driver and offers 800x600 at 24 bits, which the validation rightly refuses | tools/run_uefi.py |
| Sep 2026 | **`/home` always exists: persistent on a disk, volatile in memory without one.** It was mounted on the disk server whatever happened, so a diskless machine had no `/home` at all - and Tracker makes `/home/Desktop` when it is missing, so the ThinkPad would have booted to no desktop. Three programs had been printing "/home is in memory and will not survive" for a long time, which was an intention written in the present tense. It answers `init.lua`'s rule about writes that vanish - *worse than being told no* - by being told: the boot log, `df`, `machine` and `neofetch` all say so. The decision is taken once at boot and inherited down the spawn chain, because a process that decided for itself could disagree with the one that started it | user/init/init.lua |
| Sep 2026 | **The stick is built here rather than by `grub-mkrescue`, because the ISO failed on the machine.** It passed thirteen checks under OVMF and reached `error: file '/boot/grub/x86_64-efi/boot.mod' not found` on a real ThinkPad: those modules live only inside the El Torito FAT image, and GRUB's idea of its own root resolved to it under QEMU and to something else on that firmware. A GPT with one FAT EFI System Partition holds the loader, its configuration, its modules and the kernel - one filesystem, the one UEFI is required to read. The harness now boots the image `make usb` writes, byte for byte, because a harness that boots something else tests the harness | tools/mkusb_image.py |
| Sep 2026 | **A minimal GRUB core that loads its modules at run time, not twenty-two linked in.** With `efi_gop` built into the core, GRUB would not set a video mode at all and Kosmos came up with `none attached` on a machine whose firmware had a good panel; dropping `grub-mkrescue`'s own 200 KB binary into the identical image fixed it, so it was the core and not the layout. What must be built in is exactly the bootstrap - a module that fails to load cannot be the module that loads modules | tools/mkusb_image.py |
| Sep 2026 | **The image carries two boot headers, and the second one exists for a single tag.** Multiboot 1 boots this kernel under UEFI perfectly well - GRUB, OVMF, a firmware framebuffer, a desktop. What it cannot carry is an RSDP, and without one `acpi.c` finds nothing on a UEFI machine: it looks where a BIOS leaves the pointer and UEFI leaves nothing behind. The same image on the same four-processor machine reported four processors and drove the local APIC under `-kernel`, and reported one and fell back to a pair of 8259s through GRUB - so every line of the ACPI, APIC and MSI work was dead on the one path that matters | boot/x86_64/start.S |
| Sep 2026 | **`xorq %rbp, %rbp` ends a frame chain and wipes anything parked there.** The loader's magic was put in `ebp` at entry and read after the jump to long mode, where that instruction is the conventional way to make a backtrace stop rather than walk into whatever the loader left. The symptom was a kernel reading a Multiboot 2 structure as though it were a Multiboot 1 one and finding no memory at all. It lives in `ebx` now, which held the information pointer at entry and is free the moment `esi` has it - and the order of those two moves is itself a bug this made twice | boot/x86_64/start.S |
| Sep 2026 | **Which interrupt controller a PC has is decided when it boots, not when it is built.** q35 has an 8259 pair and an I/O APIC; a ThinkPad has both; a newer UEFI-only laptop may have only the APIC, because Intel has been removing the legacy chips for years. A kernel that hard-wired the pair gets no scheduler tick there - a boot that prints all twelve stages and then stops, with nothing else visibly wrong. `docs/targets.md` §1 already said drivers group by what the device is and which one a machine wants is a question its firmware answers at run time; this is that, for the one subsystem where being wrong is a silent hang. `opt/kosmos/irq=pic` forces the legacy path so both are exercised on every gate rather than the fallback rotting | hal/pc/irq_bind.c |
| Sep 2026 | **A PCI device reaches the APIC by MSI, because the alternative is an AML interpreter.** Under the 8259 the Interrupt Line register says which line a device uses; under an I/O APIC it means nothing, and which input the chipset's interrupt links land on is described only in ACPI's `_PRT`. The standard PCIe swizzle was tried first and is right for slots and wrong for a chipset's own integrated devices - the HDA controller being exactly one. An MSI is not a wire: the device writes a word to the local APIC with a vector this kernel chose, so there is no routing table, no interpreter, and no shared line to interrogate | hal/pc/pci.c |
| Sep 2026 | **`hal_irq_handle` says whether the tick fired.** Both trap handlers called `thread_tick` on every hardware interrupt, on the grounds that the timer was the only source - and both named the day that would stop being true. It stopped: a keyboard, a sound controller asking for a period 172 times a second, a network card. Each charged a tick nobody had spent, shortening quanta, inflating the busy and idle counts every processor meter reads, and sending core zero through a full scan of the thread table for sleepers whose deadlines had not moved | hal/hal.h |
| Sep 2026 | **A panic stops taking locks on its first line.** The panic worth printing most is the one raised inside a console write - and that one already owns the console lock, so printing it waited ten million spins for its own caller, gave up, panicked about the lock, and started again. `spinlock: console held by 0, wanted by 0`, for ever, seen twice in one afternoon on a path with no serial port. Nothing is racing with a machine that is halting, and a message on the panel is worth more than a structure that stays consistent on the way to `hlt` | kernel/panic.c |
| Sep 2026 | **`struct fb` carries the physical address as well as the pointer.** `process_grant_screen` maps the framebuffer into the compositor's address space and passed `fb.pixels`, which a page table reads as physical. That was right for two years because ramfb's pixels are guest RAM and the kernel is identity mapped, so the two numbers were equal. A firmware framebuffer is behind a graphics aperture and has to be mapped, and from that moment they are not: the window manager came up, drew a whole desktop into memory nobody was looking at, and the console stayed on the screen underneath it | kernel/process.c |
| Sep 2026 | **A framebuffer is write-combining; a device register is uncached.** `mmu_map_device` is right for a register, where the write *is* the command and merging two would be a bug that looks like flaky hardware, and wrong for eight megabytes of pixels rewritten sixty times a second. PAT slot 4 is programmed to WC and slots 0-3 keep their reset meanings, so every mapping written before this means what it always meant. Not only an optimisation: `start.S` was leaving the early framebuffer *write-back cached*, which is the one memory type MMIO may not have, and under QEMU that is invisible because TCG models no cache | arch/x86_64/mmu.h |
| Sep 2026 | **Where the page bitmap goes is a pure function, tested on the host.** `pmm_init` put it immediately after the kernel image and panicked if the memory the board reported began above that - right for every machine whose largest usable block is the one the kernel was loaded into, which is what `-kernel` gives and what firmware does not promise. OVMF reports 0x900000 at every size from 512 MB to 32 GB, so no QEMU configuration here reaches the case: the same split as `loader_fb.c`, and 23 checks that need no machine | kernel/pmm_place.c |
| Sep 2026 | **The panel gets the boot log from stage two, not stage six.** The display was the sixth of twelve stages, and the three faults that stood between this kernel and its first real machine were at three, four and five - each of them, on a laptop, a black panel with nothing to read at all. `hal_fb_early` asks for a framebuffer the firmware already set up, which only a board that needs it can answer. The identity map that makes it possible is replaced by `mmu_init`, so `hal_fb_remap` is asked the instant that returns and before the next character is printed - the first version left a three-line gap and deadlocked exactly as an unmapped framebuffer does | kernel/main.c |
| Sep 2026 | **A check that cannot be timed is asked of the machine instead.** The first test of the early screen screendumped seven seconds in, assuming the display stage had not been reached - and passed with the whole feature disabled, because OVMF, GRUB and twelve boot stages take under two seconds together; sampling every half second found no window at all. The boot log now says which of the two ways the panel got it, and the harness reads that line. The same shape of mistake as the display check that was green its whole life while broken | tools/run_uefi.py |
| Sep 2026 | **The pitch fact says which case it is rather than always saying `padded`.** A 1920x1080 panel handed over by firmware read `7680 bytes a row, not 7680: padded`, a boot fact contradicting itself in one line. It was written when ramfb was the only source and ramfb always pads. Equality is the more dangerous case and now says so: code that computes `width * 4` works perfectly on such a machine and keeps working until the machine changes | kernel/main.c |
| Sep 2026 | **Both mappings of the framebuffer carry the same memory type, and the entry is read back to prove it.** The row above programs PAT slot 4 to write-combining and `MAP_FRAMEBUFFER` uses it - for the kernel's mapping. `process_grant_screen` hands the compositor the *same physical pages* with `MAP_USER_RW`, and a memory type lives in the page-table entry rather than in the memory, so the two disagreed: write-back, which against the firmware's MTRR for a PCIe framebuffer resolves to uncached, one bus transaction per four bytes and two million of them per 1920x1080 composite. The console stayed quick throughout because the console draws through the kernel's mapping, so the machine looked like it had a slow desktop rather than a wrong page table - and boot stage six agreed, reporting the mapping that was right. `MAP_USER_FB` is asserted against `MAP_FRAMEBUFFER` at compile time and the granted entry is read back at run time, because the compile-time half says the constant is right and nothing about whether this call used it | kernel/process.c |
| Sep 2026 | **A log you can read without a desktop.** `sys.log` and the `logview` window both existed; what did not was any way to reach the ring from a machine whose desktop is the thing being diagnosed - a window is drawn by the compositor, so asking the compositor to explain itself fails exactly when it matters. `log` is a program at the prompt and needs the shell, the console and the keyboard, which were the three things still working on the first real machine. The ring went from 16 KB to 64 KB in the same change: it held less than one boot, so the twelve stages had always scrolled out before anyone could look at them, which is precisely backwards - a ring evicts by age and the boot narration is the oldest thing in it and the most valuable | user/bin/log.lua |
| Sep 2026 | **A view clamps its scroll where it draws, not only where it scrolls.** `ui.lua` clamped in its key and pointer handlers, which is every path except the one that mattered: a program setting `scroll` directly. `logview` sets it to `1 << 30` every refresh to stick to the bottom, with a comment saying the widget corrects an over-large value - and nothing did. The view scrolled a billion pixels down, every line failed the visibility test, and that window rendered *empty on every machine it has ever run on* while its own status line truthfully counted the eighty-eight lines it was not drawing. A comment describing behaviour nothing implemented, found only because a laptop with no serial port made that window the only way to read a log | user/lib/ui.lua |
| Sep 2026 | **The window manager narrates itself into the kernel's ring, always, bounded rather than switched on.** A desktop that draws once and then stops looks identical in a photograph whether the compositor is starved, the applications never asked to be woken, or their polls were never answered. Both halves of a poll are logged with the wait in scheduler ticks, so the unit bug this file has been bitten by twice would be visible at a glance. Bounded to forty passes and sixty poll lines rather than put behind a flag, because `args` is the list of programs to start - `wm trace` would try to run `/bin/trace.lua` - and because a machine that stops on its third pass is not one anybody gets to type a flag into twice | user/bin/wm.lua |
| Sep 2026 | **Windows open one to a quarter, and "taken" means hidden rather than "in the same spot".** Placement compared origins: two windows collided only if their top-left corners were within a title bar of each other, which is the right question for a cascade and the wrong one for a screen - two 850-pixel windows 110 apart pass it and bury each other by seven hundred. Four applications opening at login came up as a pile with an inch of each showing. The trigger asks only whether the *new* window is buried, because a window is entitled to overlap a panel; the quarter search asks it both ways, because a large window landing on a small one covers it completely while leaving nine tenths of itself showing. Asking it both ways everywhere sent every window fleeing the Deskbar | user/bin/wm.lua |
| Sep 2026 | **NVMe, because on the machine this is aimed at storage is NVMe or nothing.** The T14 has no SATA option at all, so `/home` lived in memory and forgot itself at every power cut. The driver is polled and single-queue: `hal_blk_read` is synchronous by declaration, so an interrupt would be a handler setting a flag the caller is already spinning on, and a second queue would be capacity nothing asks for. Everything travels through one page-aligned bounce buffer, which turns every transfer into the single-PRP case and means the PRP-list path - the part of NVMe most likely to be subtly wrong - does not exist to be wrong. Written from knowledge of the specification rather than a copy of it, so the field offsets are the part to distrust and the test that establishes them writes a file, kills the machine and reads it back | hal/pc/nvme.c |
| Sep 2026 | **A board's disk is bound like its sound and its input, not by whichever driver defined the HAL name.** `virtio/blk.c` defined `hal_blk_init` directly, which was right while it was the only disk any board had and stopped being right the moment a PC had a second candidate. `blk_bind.c` owns the HAL names and asks NVMe first, then virtio - the real device before the emulated one, so the driver that has to work on hardware is the one exercised, which is the mistake the i8042's auxiliary port spent months demonstrating. `make x86` boots from NVMe and the ARM board keeps virtio-blk, so neither is orphaned | hal/pc/blk_bind.c |
| Sep 2026 | **A display check that waits for a printed marker is waiting on the wrong thing.** The pitch phase drew three vertical bars, returned `'drawn'` for the shell to echo, and screendumped. Printing scrolls the console, and a console that scrolls repaints its cell grid rather than shifting the framebuffer - so the bars were painted over by the very line announcing they had been drawn. It had passed for years because the old console *shifted* the picture sixteen pixels instead of erasing it, and a full-height vertical bar survives a vertical shift unchanged: a check green its whole life for a reason nobody chose. There is no marker now; the phase waits for the picture, which is what `settle` exists for | tools/run_screenshot.py |
| Sep 2026 | **The window manager's trace is opt-in, because it changed what it measured.** With the narration always on, the display harness failed at its last phase: the compositor was six passes into its life several seconds after starting, and the trace itself said why - `hello-win` polls with `wait_ticks = 0`, so it is answered at once and asks again at once, and every round of that became two calls to the console server. The window being dragged did not move because the manager had not looked at the pointer yet. Three wrong explanations were tried first - placement, printing from inside `answer_waiting`, serial backpressure - and none survived the machine being asked. `wm trace` turns it on; what stays on unconditionally is six lines saying what started and where each window landed | user/bin/wm.lua |
| Sep 2026 | **The display harness drains the guest's serial line on a thread.** It drained only inside `wait_for`, so a phase that types nothing and waits on the *picture* left the pipe unread for seconds. A pipe holds 64 KB; past that the guest's write blocks, and that write is in `kputc`, which holds the console lock, which stops the machine. Invisible while the guest was quiet, and the fix belongs here rather than in a quieter guest: a test that cannot survive its subject talking will fail again the next time anything useful is printed | tools/run_screenshot.py |
| Sep 2026 | **A board asks whether its serial port is there before reading from it.** `hal/pc/uart.c` assumed COM1 because QEMU always provides one. The T14 has none; every register of a port nothing answers reads 0xFF, and 0xFF in a 16550's line status register says a byte has arrived, so every console read came back with a phantom byte, for ever. Nothing failed visibly - the keyboard is asked first and the line editor drops a non-printable byte without a word - so the shell typed perfectly while the console server took 94% of a core, and at the desktop sixty-four phantom keys a pass went into the focused window's queue and no mouse release reached an application again. Reproduced under QEMU with `-serial none`, an idle desktop going from 20% of a host core to 100%. The scratch register is the test because it is the one register that controls nothing, with two complementary patterns so that a bus reading 0xFF cannot pass | hal/pc/uart.c |
| Sep 2026 | **A full event queue gives up a keystroke or a movement before anything else, and says what it dropped.** It dropped whatever was oldest. A key or a movement is one of a stream and losing one loses one; a press, a release, a close or a resize is half of a state the application holds, and losing it leaves a widget down for ever - which is what every button on the T14 did while a phantom serial port filled the queue. Drops are logged per window, the first five and then at each power of ten, because a dropped event is invisible everywhere else. Defence in depth rather than the fix: the harness below could not make the old policy lose a release on demand, so this rule has no check of its own | user/bin/wm.lua |
| Sep 2026 | **The x86 gate boots the machine a laptop is: a PS/2 mouse, the 8259, and no serial port.** Every other boot has a COM1 and a virtio tablet because QEMU hands out both, and the T14 has neither - which is how a fault that pinned a core reached hardware with every suite green. With COM1, a click on the Deskbar through QEMU's PS/2 mouse, sent as relative counts the way a TrackPoint sends them, must open its menu in the log. Without COM1 the guest cannot be read at all, so QEMU's own `HLT=` must show the processor idling and a screendump must show the menu. The idle check was run against the build before the fix and failed, `HLT=1` in 0 of 40 samples; the menu check could not be made to fail there and is recorded as the end-to-end claim rather than as the test of that fault | tools/run_x86.py |
| Sep 2026 | **The i8042 drains on its interrupts, enables the auxiliary port in its configuration byte, and takes a byte as a packet header only with both overflow bits clear.** Three faults on the T14, each hiding the next. The configuration byte was written with `CFG_AUX_DISABLE` still set, and the TrackPoint said nothing. Bit 3 alone accepts almost every negative delta as a header - the log was full of `packet0 fd` and `fb`, movement bytes decoded as button state, presses nobody made and releases that never came. And a one-byte controller buffer was read once a scheduler tick against a device sending three bytes a report; IRQ 1 and 12 drain it now and the questions still drain too, with interrupts off in both on the one core x86 schedules. Bytes thrown away while resynchronising are logged. With all three in, QEMU's PS/2 mouse streams as well after months of sending nothing, and which of the three it needed is not isolated | hal/pc/i8042.c |
| Sep 2026 | **Pointer speed is a number in the driver and a program at the prompt, and not yet a preference.** A count moved 0.47 of a pixel at 1080p and the TrackPoint crawled; 32 units a count is about 1.9 pixels, and `pointer 48` changes it through `SYS_PTR_SPEED` - not guarded by console ownership, because it is a setting rather than the input stream. A console program because the machine it was for could not click yet, unpersisted because that machine has no filesystem of its own, and one number for a TrackPoint and a touchpad that want different curves because the driver cannot tell their packets apart | user/bin/pointer.lua |
| Sep 2026 | **A poll reply carries at most twelve events, and a reply that cannot be sent is said out loud.** On the T14 every application blocked in its first poll while the desktop drew perfectly: replies had grown past a message, `sys.reply` raised `value does not fit in a message`, and the `pcall` around it threw the error away - and a caller parked in `sys.call` has nothing left in the system that can wake it. Twelve because an event is a small table and a dozen leave room for the reply around them; deliberately not the queue's bound, since those two numbers measure different things. A partial batch is always correct, because the caller polls again at once | user/bin/wm.lua |
| Sep 2026 | **Tracker lists the mounts without asking each one for a listing first.** The sidebar sent one round trip per mount on every startup to decide whether it was worth showing, and `/net` on a machine with no network card took 18.4 seconds to answer while every other mount took four to twelve milliseconds. Tracker is the desktop, so the desktop took eighteen seconds to exist, and from outside that looked exactly like a hung window manager. A mount that cannot be listed shows empty when it is opened, which is when a person is waiting for one directory rather than for the machine. Why `/net` takes that long is still open | user/bin/tracker.lua |
| Sep 2026 | **The kernel's log is 256 KB, and each line in it carries seconds from the cycle counter.** On a machine with no serial port the ring is the whole record, and 64 KB held a boot and a few minutes of a desktop narrating itself. Stamped from the counter because `hal_ticks` does not move until the timer is armed at stage eleven, so the ten stages where boots go wrong were all `0.000`. The stamps go into the ring alone, because the screen and the serial line are what every harness matches text against | kernel/console.c |
| Sep 2026 | **The console keeps its grid while the desktop owns the screen, and only the painting stops.** The two were gated together, so every `print` made while the window manager held the screen was lost to the grid; on Control-C the console replayed a picture from before the desktop started and wrote new lines over only the cells they covered - a log with pieces of the Kosmos wordmark in it | kernel/console.c |
| Sep 2026 | **Every process that ends says so, with its name and its code.** Only a process that faulted was reported. Four applications on the T14 drew their windows and went silent, `log error` found nothing, and whether they had died at all was unanswerable from the one record the machine keeps. The line reaches the screen and the serial line as well as the ring, which a harness splitting its transcript at the word `df` found: `process 17 (df) ended` follows that command's output now, and `run_shell.py` splits at the command as typed | kernel/process.c |
| Sep 2026 | **`log word` shows the last forty matches, and never the line where the word was typed.** The console records its own echo, so `log poll` put "poll" into the ring by being run and found exactly that line: every search matched itself, and "nothing found" could never be the answer. And a search matching fifteen hundred lines scrolled them all through a console that repaints its whole grid per line, which on the machine looked like no output at all | user/bin/log.lua |
| Sep 2026 | **Key events nobody has asked for are thrown away by the console server.** The board queues every key transition for the desktop; at a shell nothing collects them, the queue filled at the first keystroke, and `hal_input_pending` answered yes for ever. The server drains them once sixty-four turns of its loop have passed with no client asking. The 100% core measured beside it on the T14 was blamed on this first, and was the serial port above | user/servers/console.c |
| Sep 2026 | **A desktop with no saved appearance is BeOS, and the top bar opens first at login.** With nothing saved - every boot of a machine whose home is in memory - the palette fell back to the neutral one on a desktop whose look is meant to be BeOS's, and the strip with the clock and the running applications was a program nobody knew to start. First rather than last because the window manager gives a strip the top of the screen, and every window opened before it would have placed itself a bar too high. The display harness names the kit's dark palette in `/home/.appearance`, because nearly every colour it looks for is a field of that palette and moving them is not a change of numbers - BeOS's idle tab is the same grey as every raised button. So BeOS is what `make shot` photographs and what every x86 desktop boot runs, and not what the harness colour-checks | user/lib/startup.lua |
| Sep 2026 | **A window counts as polling from the moment it opens.** `collect_dead` closes a window that has gone silent and whose process is gone, and it skipped any window with no `last_poll` - which is exactly the window of a process that died before its first poll, left on the screen for good | user/bin/wm.lua |
| Sep 2026 | **The window manager says what it launched, where each window and menu went, what each window was renamed, and which window it closed because its process was gone.** A machine with no serial port is where these questions get asked, and a photograph of a screen cannot answer any of them afterwards. A menu is named as its owner's, because `wm: window window at 812,74` was what a working click looked like. A rename to the name a window already has is not said, or `is now` would report something that did not happen | user/bin/wm.lua |
| Sep 2026 | **Choosing the drive is the confirmation in `mkusb.sh`.** Typing out the drive's name was removed on purpose: the script runs several times an hour while a machine is brought up, and the friction bought nothing the refusals do not. Those stay - only external physical drives listed, the answer checked against that list, internal drives and the running system's disk rejected by separate routes - and the header says what is lost: with two sticks plugged in, `disk4` for `disk5` erases the other one | tools/mkusb.sh |
| Sep 2026 | **An I/O APIC is as large as its version register says, and a refused APIC says why in the boot log.** `apic.c` rejected any I/O APIC with more than sixty-four inputs as "an impossible size" - on an eight-bit field - and the chipset in a ThinkPad T14 Gen 2 has a hundred and twenty (`GSI 0-119` in a Linux boot log from that machine). So the first real machine never left the 8259 pair, and the reason sat in the driver's description while the boot log printed the 8259's, at every boot since the laptop first ran it. The check now refuses only a chip that reads all ones; the local APIC's mode is read from IA32_APIC_BASE before any of its registers are touched, so a firmware that left x2APIC on is refused with a sentence rather than a hang; and every fallback names its reason. No QEMU configuration has a 120-input I/O APIC, so the decode is a pure function tested on the host with the T14's register value | hal/pc/apic.c |
| Sep 2026 | **On x86-64 a core's own state is behind the GS base, and `swapgs` guards every boundary with ring 3.** `cpu_self` was a static, honest for one processor, and the TSS and the syscall entry's scratch word were one each for the machine - so two cores entering from ring 3 at once would have landed on one stack and written each other's stack pointer. Each core now has a block holding its `struct percpu` pointer, that scratch word and its own TSS, installed in IA32_GS_BASE; `swapgs` is the first instruction of `syscall_entry`, the top of `isr_common` when the saved CS says ring 3, and the last instruction before `sysretq` and `iretq`, each in a window with interrupts masked. GS holds the null selector from boot on, because loading a selector into GS replaces the base - which is also why `enter_user` stopped loading one. Landed on one core first, where every entry path was exercised and nothing could change but the mechanism: 57 x86 checks and 136 guest checks | arch/x86_64/gdt.c |
| Sep 2026 | **An x86 processor is started in two halves: the board's trampoline to flat 32-bit mode, and the architecture's climb from there.** A STARTUP IPI begins a processor in real mode on a page under 1 MB, and reaching flat protected mode from there is how a PC starts one - so `hal/pc/trampoline.S` does that and jumps to an address it was handed. Long mode is how this architecture reaches C, and `boot/x86_64/start.S` already made that climb for core zero, so `_secondary_start32` makes it again on the same boot page tables - the kernel's map the low megabyte non-executable, and the instruction after paging comes on is fetched from that page. The page is used only where the loader's memory map called it RAM, recorded during the boot walk because the map is gone by the time a core starts. INIT and STARTUP go to the local APIC id the MADT listed, with the running processor taken out of the list, because core zero is whichever one the firmware booted | hal/pc/trampoline.S |
| Sep 2026 | **Every x86 core ticks on its own local APIC timer, and only core zero does the machine's half of a tick.** A second core with no timer and no way to be woken sits in `hlt` for ever, and the guest suite's first SMP check puts a thread on core 1 - so starting cores and giving them a clock were one step, not two. Each core arms its timer at the rate core zero calibrated against the 8253 and counts its own ticks, which is what `hal_ticks_on` reads to tell a live secondary from a dead one. Waking input sleepers, the audio server and the console's cursor stay on core zero, the guard `arch/aarch64/trap.c` already kept. A wake is a fixed IPI on vector 0x3E whose handler is its acknowledgement. Placement across cores stays off on this board until there is a TLB shootdown, which AArch64's broadcast invalidates make unnecessary and x86 does not have | hal/pc/apic.c |
| Sep 2026 | **Every x86 processor asked about gets a line in the boot log, whatever became of it.** The first real machine counted eight processors, started none, and said only `0 of the others in the kernel too` - which is what a board that refused, a processor that never ran and one that died on the way in all look like. So `cpu_on.c` names a refusal, and a processor it starts writes a stage number into a word in the trampoline page as it climbs - the trampoline, `start.S` and `entry.c` each write theirs, handed the word's address in `esi` - which the board reads back. It reads the page back too, before sending anything, because a map calling a page RAM is a claim. A processor that reaches the kernel says how long that took, and `kernel/smp.c` adds `never arrived` for one that got that far and no further. Checked both ways under QEMU: INIT aimed at APIC ids nothing answers, and a map without the page | hal/pc/cpu_on.c |
| Sep 2026 | **x86 answers a changed mapping with a TLB shootdown, and a core waiting for a lock answers it too.** AArch64's `tlbi ...is` is broadcast by the hardware; x86 has no such instruction, so a translation changed on one core stays cached on every other with those tables loaded, and a stale entry is a successful read of somebody else's page. `as_unmap`, an `as_map` over a present entry and a kernel unmap are each followed by a round: the other cores that may hold it get the range and the wake IPI, and the sender waits. A core publishes its root before writing CR3 and a round reads roots after changing entries, so one of the two sees the other. One round at a time for the machine. Because every lock masks interrupts, `spin_lock`'s wait calls `cpu_lock_wait`, which on x86 answers rounds - otherwise a core blocked on a lock the sender holds would never take the IPI. The guest test passed with the shootdown switched off until its reader ran with interrupts masked; now it fails without it | arch/x86_64/mmu.c |
| Sep 2026 | **The PC board's drivers lock, because placement puts system calls on every core.** `i8042.c` had written it down - "a second core would need a lock here" - and a spread desktop proved it: the window manager polling from one core while IRQ 12 drained on another. Locked with it: PCI configuration's 0xCF8/0xCFC pair, the CMOS index/data pair, fw_cfg's directory walk (a select, then reads that advance a position the device keeps), the UART's status-then-data read, and NVMe's one queue pair and bounce buffer - per command, so a large read never holds a lock that masks interrupts for its whole length. The virtio drivers both boards share took theirs in 0.9.20 | hal/pc/i8042.c |
| Sep 2026 | **A PC reads boot options from the loader's command line when there is no fw_cfg.** Every option - `opt/kosmos/boot`, `irq`, `smp` - was a `-fw_cfg` argument, so a laptop could be given none. `hal_boot_option` is each board's now: QEMU's ARM board asks fw_cfg; the PC asks fw_cfg and then the command line Multiboot 1 or 2 carried, copied during the memory walk. So GRUB's `multiboot2 /boot/kosmos.bin opt/kosmos/smp=8` works on the machine, and `-append` tests the same path under QEMU | hal/pc/boot_option.c |
| Sep 2026 | **Eight processor slots.** Four was what `make test` boots and what a Pi 5 has, and the ThinkPad started three of its seven others and left four in firmware. A slot costs an idle thread from the pool of 48 and two 4 KB stacks; the machine is still asked how many it has and the smaller number wins | kernel/percpu.h |
| Sep 2026 | **A caller joins the reply queue before its receiver is woken, because on another core the receiver answers at once.** `ipc_call` handed the message over and woke the receiver, and only then joined the reply queue and named its endpoint - while `ipc_reply` reads that name with no lock, since it is how it finds the lock to take. On one core the woken receiver cannot run first. On two it can: it reads NULL, refuses the reply as addressed to nobody, and the caller waits for ever. Found by reading the IPC paths while chasing a spread x86 desktop. `smp: a reply reaches a caller on another core` puts a server and a caller on two other cores and fails with the old order within 111 rounds of 2000, one reply refused; it passes on both boards. With the fix the ARM display harness's editor phase - the failure that kept placement off - passes under `SMPWORK=4`, and with the old order put back it fails again, so that was the failure | kernel/ipc.c |
| Sep 2026 | **A dead thread's slot is not handed out until its processor has left it.** `thread_exit` marked a thread dead and then switched away, and until that switch was over the thread was still on its stacks with its registers unsaved. `alloc_thread` reuses dead slots, stacks included, from any core - so a thread created in that window was given the slot, had its context planted, and then had the dying thread's registers saved over it: it started life inside `thread_exit`, which panicked with `a dead thread was scheduled`. Once in eleven x86 guest-suite runs, and it stopped this revision's gate. The pool's own comment said a dead thread "stopped the moment thread_exit switched away", which was true while nothing could run in between. `thread_exit` now names the thread in its processor's `leaving`, under the pool's lock, before marking it dead; the thread that arrives next lets go, in `thread_switch_finished`, which `switch_into` and both `thread_entry`s call first; and the allocator passes over a slot any processor names - the first code in this kernel that runs on the far side of a switch. `smp: a slot is reused only once its thread has left` forces the window: without the fix it panics with that message in two runs of two on each board, and with the fix it passes on both | kernel/thread.c |
| Sep 2026 | **Asking whether there is a keyboard no longer re-initialises the keyboard controller.** `SYS_SYSINFO` calls `hal_keyboard_init` every time - Processes, Monitor, the top bar and the window manager ask several times a second - and its comment called that idempotent. On the i8042 it disabled both ports, read and discarded whatever was waiting, and rewrote the configuration byte. On one core that throws away the rest of any mouse packet in flight, which the driver then resynchronises past; with threads spread it also raced IRQ 12's drain for the configuration byte it was waiting on. Both init functions now answer from what they found the first time, under the driver's lock. ARM's virtio init already did | hal/pc/i8042.c |
| Sep 2026 | **An x86 IPI is sent with interrupts masked across its two register writes.** The destination goes into ICR high and the command into ICR low; an interrupt between them whose handler sends an IPI of its own rewrites the destination, and the first goes to the wrong core - a wake lost with nothing to show for it. AArch64 sends an SGI with one system register write | hal/pc/apic.c |
| Sep 2026 | **`.gitignore` names the generated directory by its path, `/build/`, and never by its name.** Without the leading slash, `build/` matches a directory called `build` at any depth, and upstream projects keep sources under that name: NetSurf's libparserutils, libhubbub and libcss each ship one, and two of them hold the tables `aliases.inc` and `entities.inc` are generated from. So nine vendored files were in no commit from the day they were vendored, and nothing could say so - the checkout that vendored them still had them, and a pattern that hides a file from a commit hides it from `git status` too. The first thing in a while to start from what was actually committed was a worktree, and there the full image stopped at its first perl script; `make test`, `make prepush` and `make release` all build that image. `CLAUDE.md` already said everything generated lives under one top-level `build/`, so the unanchored form never bought anything | .gitignore |
| Sep 2026 | **New threads spread across every processor that arrived, by default.** Placement was off for months - `kmain` called `thread_place_across(1)` whenever no boot option said otherwise - and what held it off was a series of faults rather than an opinion: unlocked drivers, the preemption path, a reply lost between cores, and on x86 no TLB shootdown. With those fixed the ThinkPad ran its desktop across all eight of its processors with a pointer that did not jump, so a plain boot now leaves the count at "however many are online". `opt/kosmos/smp=N` stays as a way to narrow, and is capped at the processors that arrived: a number somebody typed could otherwise home threads on cores that never started, where they would never run. The guest suite still pins itself to core zero, because a dozen of its checks drive three threads by yielding and only mean anything if those threads are here | kernel/main.c, kernel/thread.c |
| Sep 2026 | **A process is `exited` only once it has given everything back, and its parent waits for it under the same lock.** `process_exit` set `exited` on its first line and woke the parent with nothing held; `process_wait` scanned, named itself the waiter and blocked with nothing held either. On one core neither order could be seen. On a spread machine a parent could reap a slot whose owner was still releasing its pages - and the next spawn be handed it mid-teardown - or sleep through a wake that arrived before it had blocked, which is the `sys.wait` the shell makes after every foreground command. `exited`, the exit code and the cleared thread are published last under `processes_lock`, with the wake; the wait scans, reaps and blocks under it through `thread_block_and_release`, as IPC does. Found while chasing why `make stress` failed the first time it booted four processors | kernel/process.c |
| Sep 2026 | **`make stress` boots four processors, and waits for each child before counting.** It passed QEMU no `-smp`, so the release gate had only ever stressed one processor. And it counted the moment `run` returned - which is when the program replies, before its process has exited: on one core the child was gone by then, on four it was still leaving on another core, one thread and about 650 pages over at every snapshot | tools/run_stress.py, user/bin/stress.lua |
| Sep 2026 | **Lite XL keeps its own loop, and Kosmos does its part inside the loop's waits.** The plan was to drive `core.run()` a frame at a time from Lua, as Doom is driven, and it cannot be: the scheduler for Lite XL's background work - the cursor blink, highlighting, a project scan - is a local inside `core/init.lua`, so stepping frames from outside would never run it. So `core.run()` runs unchanged, and `system.wait_event` and `system.sleep` show the frame just drawn and then ask the window manager what happened, never for longer than a quarter of a second. A close from the desktop arrives as Lite XL's own `quit`, so the window still closes. The rest has the same shape: keys by SDL's names in SDL's order, `io.open` over the namespace, the installed tree worked out from `/lib`'s keys, and a direct window's two buffers kept in step by copying what was committed | user/bin/litexl.lua, user/lib/litexl_system.c, user/lib/litexl_sdl.c |
| Sep 2026 | **What Lite XL's launcher decides without asking anybody is a library, checked on the build machine.** Paths, the installed tree, files, the event queue, key names and the damage rectangle are plain Lua in `user/lib/litexl_host.lua`, and `tools/test_litexl_host.lua` checks them in `make test`. The fault that made it: the queue consumed events by clearing slots, so once drained its count went below zero and the next events - a Control release among them - were written where nothing read them, and the editor took `l` and `o` for `ctrl+l` and `ctrl+o`. Found with `wm litexl:--trace`, which prints every key, event, command and log line; with the old consumption put back the test fails. `make litexl-check` boots a `LITEXL=1` image and reads back the files the editor saved | user/lib/litexl_host.lua, tools/test_litexl_host.lua, tools/run_litexl.py |
| Sep 2026 | **Lite XL's own faces are carried in a `LITEXL=1` image, in a table of their own, with their terms recorded beside them.** They were stood in for at first, because `FiraSans-Regular.ttf` and `icons.ttf` are in the vendored tree with no licence beside them. `LICENSE.FiraSans` is transcribed from Lite XL's `licenses/licenses.md`, SIL OFL 1.1. `LICENSE.icons` is Kosmos's, since upstream wrote none, and gives its evidence with the terms: Fontello in the font's name table, a Lite XL maintainer saying the icons are Font Awesome 4 - whose fonts are SIL OFL 1.1 - and five glyphs named for Lite XL. The table is `litexl_fonts_table` rather than `fonts_table`, because `gfx.fonts()` offers every face in that one and Appearance would list `icons` as a face for the desktop. The stand-ins and the `/home/fonts` fallback went with the reason for them | Makefile, user/lib/litexl_system.c, runtime/upstream/lite-xl/data/fonts/LICENSE.* |
| Sep 2026 | **`make prepush` runs `make litexl-check`.** The gate never built a `LITEXL=1` image, so nothing on the way to a push would notice the editor breaking. It runs before `shot`, because it leaves a lean `LITEXL=1` image in `build/kosmos.elf` and `shot` builds the ordinary one again before its picture. It costs the gate a build and two boots | Makefile |
| Sep 2026 | **Quake is `make QUAKE=1`, built from Chocolate Quake rather than quakegeneric.** quakegeneric was the plan and says it builds only for 32-bit machines; Kosmos is only 64-bit, and Quake keeps pointers in 32-bit integers in places, so using it would have meant patching a vendored tree. Chocolate Quake reproduces the same WinQuake 1.09 and ships arm64 builds. libretro's TyrQuake is 64-bit clean too, but thirty-two of its engine files include `libretro-common` headers directly. Every Chocolate Quake source file carries id's version-2-or-later notice, and the flag keeps it out of `FULL=1` for Doom's two reasons, the licence and the size | runtime/upstream/quake/, Makefile |
| Sep 2026 | **Quake's engine is compiled unmodified and its platform replaced, not patched.** `user/lib/quake_kosmos.c` stands where `sys.c`, the SDL video, input and sound, the UDP drivers and `main.c` stood. `user/lib/quake/` answers the engine's own `#include <SDL.h>` with eight integer types, fifteen libc names, a timer and two network types. A forced header points `unlink` at `remove` and makes the `-condebug` log's `open`, `write` and `close` do nothing, rather than adding a POSIX name to Kosmos. Quake's headers go on the path with `-iquote`, because `kernel/` also has a `console.h` and a `screen.h` | user/lib/quake_kosmos.c, user/lib/quake/ |
| Sep 2026 | **A region may be 32 MB.** `MEMOBJ_PAGES_MAX` was 16 MB, "a double-buffered full screen", and Quake's shareware pak is 18.3 MB, so it was refused as "no room". What bounds a process is the 48 MB it may map; doubling the region cap costs every descriptor eight index pointers, 16 KB of .bss across the pool, and no pages until a region that large exists | kernel/memobj.h, tests/tests.c |
| Sep 2026 | **An engine whose frames outgrow the stack gets a stack of its own, not every process a bigger one.** Quake's `R_EdgeDrawing` is a 205 KB frame and a process's stack is 256 KB - one contiguous run that every process pays for, which is why it was not raised. `quake_kosmos.c` maps a megabyte and a page, unmaps the lowest page - addresses are never reused, so it stays a guard - and runs `Host_Init` and `Host_Frame` there through `kosmos_call_on_stack`, thirteen instructions per architecture | runtime/libc/callstack-*.S, user/lib/quake_kosmos.c |
| Sep 2026 | **The scanf family has one scanner, bounded by a length, and `%f` is a `float`.** Quake reads a demo's CD track with `fscanf`, and a demo is a file inside a pak, followed by the next file rather than a NUL - so the scanner in `misc_user.c` moved to `runtime/libc/scan.c`, takes a length and says how much it consumed. Moving it found `%f` storing a `double`, which Doom's `%lf` never showed and Quake's `%f` into a `vec3_t` would have. `tools/test_scan.c` checks both on the build machine | runtime/libc/scan.c, runtime/libc/stdio.c, tools/test_scan.c |
| Sep 2026 | **A display check clicks where the window manager says a window is, not where the window asked to be.** The clipboard phase opens `machine` and the gallery together, and its clicks were numbers written for the gallery at the top left, where it asks to go. That spot is under the report, and the window manager moves a window that would be more than a third buried - so whenever `machine` got its window first the gallery opened bottom right, the clicks landed in the report, and Control-W v pasted into a read-only editor. "Changed nothing", four runs in a row while gating 0.10.25, and a clean 0.10.24 passing once made it look like a regression; with every screendump saved, 0.10.24 failed and 0.10.25 passed. Which process gets its window first is a race. The line the window manager prints for every window it places, kept on unconditionally for questions like this, is where the clicks come from now; with the report forced first, the old clicks fail and the new ones pass | tools/run_screenshot.py |
| Sep 2026 | **LICENSE is the one record of whose work is in Kosmos, and About reads it out of the image.** About Kosmos ends with a Licences section built from the image's own copy - `sys.asset("LICENSE")`, carried in the asset table like the ASCII banner - rather than from a list of its own that would fall out of step with the file. `user/lib/licences.lua` reads the shape LICENSE keeps: an entry at two spaces, its details at six, notes at the margin, anything else reported as stray. `tools/test_licences.lua` in `make test` holds the file to the tree - every directory under `runtime/upstream/` and `lua/upstream/` has to be named in an entry - and it found three that were not: musl's maths, NetSurf's five libraries and the Tango icons | LICENSE, user/lib/licences.lua, user/bin/about.lua, tools/test_licences.lua |
| Sep 2026 | **An ordinary image is a GPLv2 work, because it carries Doom, and `FULL=0` is the MIT one.** LICENSE and Doom's README said Doom was built only under `make DOOM=1` and that an ordinary `make` produced an MIT binary, while `FULL ?= 1` had put Doom in every ordinary build since `make qemu` became the whole system. Diego decided the text follows the build: the machine you run is the whole machine, and there is no dynamic linking to keep a GPL engine apart from the rest of it | LICENSE, runtime/upstream/doom/README.md, Makefile |
| Sep 2026 | **`make MEGA=1` is everything in one image: `FULL=1`, Lite XL and Quake.** For running the whole of it at once rather than for the suites - the test and bench images ignore it, and the checks that build a variant of their own pass `MEGA=`. The first MEGA link failed on twenty-four names Doom and Quake both define - `Z_Malloc`, `R_Init`, `S_StartSound`, `deathmatch` - so Quake's copies are renamed in its forced header, which every Quake file sees, and neither vendored tree is edited. Always rather than only in MEGA, so `make quake-check` boots the Quake a MEGA image carries. `make prepush` links a MEGA image, which is where the next collision would show | Makefile, user/lib/quake/kosmos_quake.h |
| Sep 2026 | **A userland image has to end before its heap, and the link says so.** `USER_HEAP` is fixed sixteen megabytes above where a process's image starts, and nothing checked that the image ends first. `user/user.ld` asserts it now. The ordinary image is 6.12 MB and a MEGA image 8.64 MB; given eight megabytes instead of sixteen, the MEGA link stops with the message | user/user.ld |
| Sep 2026 | **A disk can arrive with the kernel: GRUB loads it from the stick, and Kosmos mounts it at boot.** The T14 boots from a USB stick Kosmos cannot read - there is no xHCI driver - and its NVMe drive holds neither Doom's WAD nor Quake's pak, so `make MEGA=1 usb` gave a laptop with both games and no data for either, while `make qemu` ran Doom fine off `build/kosmos.img`. `make x86-usb-image` now copies that same disk onto the stick when `kfs.lua` can read it, `grub.cfg` loads it with `module2`, and `hal/pc/memdisk.c` presents the memory as the board's disk. Writes to it are gone at power-off. Rejected for now: a USB storage driver, the largest item left in `thinkpad.md` | hal/pc/memdisk.c, tools/mkusb_image.py, Makefile |
| Sep 2026 | **The loader's disk is bound ahead of a real drive, and its pages never reach the allocator.** It exists only when somebody put it on the stick for this boot, so it is what the machine mounts, and on such a boot the drive's `/home` is not mounted - `blk_bind.c` says so rather than leaving it to be found. A loader puts a module wherever it likes, which under QEMU is inside the largest usable region, so that region becomes whichever side of the module is larger. The module is always mapped into the device window by `mmu_map_ram`, cached: `mmu_map_device` would have been correct and far too slow for bytes. The boot log names it, which is how a machine with no serial port says GRUB handed it over | hal/pc/blk_bind.c, hal/pc/memory.c, arch/x86_64/mmu.c, kernel/main.c |
| Sep 2026 | **A launcher is an empty file whose attributes say what to start.** `kind=launcher`, `program`, `args`, and an `icon` if it wants one. Tracker opens it by sending the window manager the `launch` the Deskbar sends, so a launcher starts nothing the Deskbar could not and the window manager's check on the name stays the only check. `launcher path program arguments` makes one at the prompt, and Drive - Tracker at `/` - is one. Rejected: links, which kfs does not have and which name a file rather than start a program; and a special case in the desktop, which its own rule - it shows `/home/Desktop` and nothing else - forbids | user/bin/launcher.lua, user/bin/tracker.lua, user/lib/files.lua |
| Sep 2026 | **A desktop icon is where it was dragged, and the place is an attribute of the file.** `desktop_x` and `desktop_y`, which is BeOS's arrangement: the place moves with the file and goes with it, and no positions file beside the folder can fall out of step. An icon never placed takes the next free cell down the first column, and `iconlayout.lua` works that out on the build machine, where it is tested without a screen | user/lib/iconlayout.lua, user/bin/tracker.lua |
| Sep 2026 | **The desktop is the screen less the strip.** The backdrop asks for the whole screen, which is all it knows; the window manager sizes it from what the strip claimed and refits it, with a `resize`, when the strip starts later or closes. Closing a strip had never been recounted, so a top bar that quit kept its room. A desktop that draws from its own top-left corner is then below the bar without knowing there is one | user/bin/wm.lua |
| Sep 2026 | **Deleting moves to a Trash on the desktop, and only the Trash deletes.** A folder rather than a flag per file, because a folder is already something a window can open, list, drop onto and take things out of. A name it already holds gets a free one - a second `notes.txt` is `notes 2.txt` - and Empty Trash, which is the one delete that cannot be taken back, is asked for by name in the File menu. No confirmation, for the reason there is no dialog anywhere in Tracker | user/lib/files.lua, user/bin/tracker.lua |
| Sep 2026 | **Haiku's icons, and each program declares its own.** Forty-eight of `darealshinji/haiku-icons`' 32x32 PNGs, MIT, pinned to a commit, in place of seven of Tango's. `-- kosmos: icon App_Tracker` in a header is reported by `/bin` beside the section and drawn by the Deskbar; a menu with pictures gives every row the picture's height, because nothing here scales one. The Haiku logo and leaf are trademarks and neither is among them | assets/icons/, user/servers/binfs.c, user/lib/ui.lua, user/bin/deskbar.lua |
| Sep 2026 | **A name anywhere in Kosmos may be sixty-four characters.** kfs allowed 255 already. `/bin` and `/lib` held 48 and `/ramfs` held 128 bytes for a *whole path*, so a 64-character file two directories down did not fit at all. Now 64 and 256 - and a `/ramfs` listing, query or watch answers four paths a message rather than eight, because eight 256-byte paths are the whole message | user/include/binproto.h, user/include/ramproto.h, user/init/init.lua |
| Sep 2026 | **The desktop is started by the window manager rather than by `run`.** `desktop.lua` started Tracker itself, so Tracker had to look `/app/wm` up by name - and a window manager stopped with Control-C never unregisters, so after a second `wm` the name still pointed at the dead one and Tracker died with "no such path: /app/wm". The desktop of the second `wm` in a boot had never worked. A launch through the window manager hands the child the live endpoint, which is how everything else started from the desktop gets it, and it is the right way round whatever the registry does. The registry keeping a name whose holder had gone was the other half, a separate bug, and the row below is where it was fixed | user/bin/desktop.lua |
| Sep 2026 | **The desktop hides nothing: it is transparent between its icons.** Tracker's backdrop filled itself with the desktop colour - the same colour the compositor paints when nobody has chosen a picture - so it looked right and a wallpaper never appeared. It was worse than covered: `compose_rect` paints that layer only where no window reaches, and a screen-sized opaque backdrop meant the picture was never drawn at all. The backdrop now occludes nothing, is blended rather than copied, and its labels carry a shadow instead of a filled box, because a box of the desktop's colour behind every name is what that looks like on somebody's photograph | user/bin/wm.lua, user/bin/tracker.lua |
| Sep 2026 | **The cheat sheet is in two halves: at the prompt, and on the desktop.** It listed commands only, and half of what this system does now is dragging, dropping and opening things no command names - launchers, the Trash, where an icon's place is kept, which key the window manager keeps for itself. The desktop half is written the same way as the first: what it does, in the words on the screen | docs/cheatsheet.html |
| Sep 2026 | **An endpoint ends with the process that made it, and an `/app` name lasts as long as its endpoint.** The window manager stopped with Control-C destroys its endpoint and never unregisters, so the registry kept `wm`: the next window manager was filed as `wm2`, and the Tracker `desktop` starts looked `wm` up, was handed a capability that named nothing, and died with "no such path: /app/wm". A killed process was worse, because nothing destroyed its endpoints at all: a client waiting on one waited for ever, and a lookup would have handed out an endpoint nobody would answer. `process_exit` now destroys every endpoint the process created, and `appfs` asks the new `SYS_CAP_CHECK` about each capability it holds before it answers anything, dropping the dead. The maker rather than the receiver, so a server that dies leaves its endpoint to whoever made it, the kernel or init. Rejected: `wm.lua` unregistering on its way out, which a kill never reaches; the kernel telling the registry when a process dies, which needs a notice the kernel cannot send without blocking and names processes by a global id; and the registry calling an application to see whether it answers, which would make it a process any hung application can stop | kernel/ipc.c, kernel/process.c, user/servers/appfs.c, design.md §4.2, testing.md §18.20 |
| Sep 2026 | **The Deskbar is a menu of launchers, and a thing is in the menu because a launcher file exists.** `/home/Deskbar` holds a folder per section, each holding launchers; a folder inside one is a submenu. It was `/bin` filtered by an attribute, with each program's header naming its section - which made the menu read-only by construction, because `/bin` is an array the build puts in the binary: moving 3dcube out of Demos meant editing source and rebuilding, and an item could carry no arguments at all. Moving an item is now moving a file, renaming it is renaming a file, and giving it arguments is an attribute - all of it Tracker's job. `kosmos: section` is the default a program is filed under the first time the tree is made, and nothing more. Rejected: a fallback listing programs with no launcher, and a ledger of names ever seen - both exist only to patch the fallback, and a launcher thrown away has to stay thrown away | user/lib/deskbarmenu.lua, user/bin/deskbar.lua |
| Sep 2026 | **One bar across the top, and `topbar` is gone.** The Kosmos menu at the left, a button per running window with its application's own picture, and the indicators at the right - 36 pixels with 32-pixel icons, because nothing here scales one and any other number would be a crop. There were two pieces of always-present chrome: a Deskbar in the top-right corner and a top bar whose five shortcuts were a hard-coded list, which is a menu that cannot be edited sitting next to a menu that can. BeOS put its Deskbar in a corner and this is the one thing taken from Windows instead: a person hunting for a window wants one place that is always the same shape. A second click on the focused window's button minimises it, which is the only gesture on the bar that has to be learned | user/bin/deskbar.lua, user/bin/wm.lua |
| Sep 2026 | **The Deskbar holds `network` and `audio`, and that is a change of position.** It declared `needs screen` and nothing else, on the argument that launching goes through the window manager so that reaching the Deskbar is not reaching everything. That still holds for *power* - `processes` stays with the window manager and Restart is a request the menu sends - but the bar is where a person now manages the machine from, and a bar that cannot see the volume cannot show it. **Reading state is not holding power**, and the two are kept apart deliberately. Neither widens anything on a machine without the hardware: `may_pass_audio` grants nothing without a sound card, so `/dev/audio` is absent from that namespace and the speaker is not drawn - the old rule about never drawing an indicator for a subsystem that does not exist is now enforced by the kernel rather than remembered in a comment | user/bin/deskbar.lua, user/bin/wm.lua |
| Sep 2026 | **A launcher is a file *type*, and what it records is explicit.** `type=launcher` is written beside `kind`, `filetypes.lua` reads either so the thirty-eight already on a disk needed no migration, and Tracker's Kind column says `launcher` - it called `kind_of` without the attributes, so it could only ever read an extension and every typed attribute the disk grows would have been invisible. The `program` attribute is the whole path: `handlers.launch` completes a bare `doom` to `/bin/doom.lua`, which is right for somebody typing and wrong in a file, because what a launcher records should say what it runs without the reader knowing a rule the file cannot state. The completion stays in the typing | user/lib/filetypes.lua, user/bin/launcher.lua, user/bin/launcheredit.lua |
| Sep 2026 | **The right button reaches an application only if it said it understands `button`.** Both pointer drivers have always reported it and everything above threw it away. Delivering it is dangerous because a program that has never heard of one reads it as a left press and acts: Paint would draw, Quake would fire, Lite XL would move its cursor. A guard in each of those is the trap CLAUDE.md names - a rule that requires you to recognise a third case will miss the fourth - so the window manager sends a right press only to a window that declared `context`, `ui.lua` declares it for every window it opens and drops what no view claimed, and the three that read `mouse` themselves never declare it and cannot be bitten | user/bin/wm.lua, user/lib/ui.lua |
| Sep 2026 | **The gate compiles in parallel and stops rebuilding the kernel for flags it does not use.** `prepush` was five prerequisites, so make built every object one at a time on a ten-core machine; `-j` on the whole thing would run five QEMUs racing for the same build directories, so the ordering stays in the recipe and the parallelism goes inside each stage. And there was one flags stamp carrying every flag in the build, in `$(BUILD)` - which is only ever `build`, `build/test` or `build/bench` and does *not* vary with DOOM, WEB, LITEXL or QUAKE, while `UBUILD` does. So plain to MEGA to plain rewrote it twice and recompiled the whole kernel three times a gate for userland flags. Two stamps now, each covering the flags its own objects are built with | Makefile |
| Sep 2026 | **The build writes down what it put in the userland image, and the kernel asks four times a boot whether that is still what is there.** A ThinkPad boots every stage and then `process 8 (diskfs) ended, code 1`; the same kernel with an 8 MB loader disk instead of 64 MB is perfect, and no QEMU configuration here reproduces it. The first thing missing was a way for the server to speak: spawned with one capability it owns no console, so `print` is refused and so is the `say` in `main.c` that reports a Lua error - a Lua error there had been invisible since it was written. Staged exit codes made it say **15, a syntax error**, which is the same fault as `beep`'s `audio.lua:1: unexpected symbol near '$'` on that laptop: the Lua source of `/lib` arrives corrupted. Those bytes are the one blob every process runs out of - `process_create` maps the image's read-only half from the kernel's own copy, below the region the allocator hands out - so nothing can write them and something does. `bin2c.py` now emits a checksum of the blob and one per page, and `check_init_image` asks as the loader left it, with the page tables built, with the devices up, and before init: four points in one boot rather than three boots bisecting, because a boot of that machine is expensive. The loader's whole memory map is printed beside it - `consider` kept type 1 and threw the rest away, so "what does the firmware say is at this address" had no answer. Not a fix: an instrument, with a host test over a fixture the real script generated, because its two halves are Python and C and a canary that cries on a healthy boot is worse than none. **On its first boot it found that this kernel is loaded over a megabyte the firmware asked to keep** - three type-4 entries between 0x800000 and 0x900000, with the userland image on top of them - which nothing had ever checked, because the multiboot header asks for a fixed megabyte and the image goes there whether or not the firmware agrees. It survives under emulation, so that is a lead rather than a conclusion, and the next boot of the T14 settles it: the first changed page's address either falls inside one of those entries or it does not | tools/bin2c.py, kernel/image_sum.h, kernel/main.c, hal/pc/memory.c, tools/test_imagesum.c, testing.md §18.22 |
| Sep 2026 | **USB is the next milestone, and it was chosen by a machine rather than by a wishlist.** The ThinkPad will not boot with a 64 MB disk - and the disk is 64 MB of *memory* because Kosmos cannot read the stick it booted from, so GRUB carries the whole filesystem in as a module and on that laptop the image arrives with eleven contiguous pages corrupted before the kernel runs. The answer is to stop needing the loader: mass storage over USB removes the module, the relocator and the ceiling on what can be carried. The same stack is also the shortest path to a network on metal - the T14 has no RJ45 and its WiFi is an AX201, a CNVi part with the MAC inside the chipset and no public documentation, where a USB-C Ethernet adapter needs no firmware and no crypto and ARP, IP, TCP and DNS have never once run outside emulation. Haiku does drive that card, by wrapping FreeBSD's driver, which for a card this new is Linux's under LinuxKPI - two compatibility layers, which is a real option and a separate decision rather than this one. Rejected: wired Ethernet first, which assumed a port this laptop does not have | roadmap.md, state.md, thinkpad.md §6a |
| Sep 2026 | **A driver is a server, not kernel code - and the four pending ones do not share a source.** The question was whether to vendor FreeBSD drivers under a compatibility layer the way Haiku does. Haiku runs them *inside its kernel*, which is right for a monolithic one and impossible here: the FreeBSD KPI assumes `malloc(9)`, sleeping locks and kernel threads, and a kernel with an allocator is the thing this project has refused from the start. A microkernel has somewhere better to put them. A driver at EL0 in its own address space has a bug that is a crash rather than a compromise, and may have an allocator and threads *because it is a process*. What is missing is three capabilities - map a device's registers, get memory a device can reach and its physical address, receive an interrupt - which is perhaps 800 lines and the largest addition since capabilities. `SYS_DISK_READ` being a syscall at all is the tell that this was unfinished. And the sources differ: NVMe is **already written** and merely untested on the laptop; audio wants **Haiku's own** HDA driver, which is native rather than FreeBSD's, read beside ours to fix one graph walk; USB is **written from the public xHCI spec**, no firmware and no crypto; and WiFi wants **OpenBSD's `iwx`**, which names this exact CNVi card, rather than FreeBSD's, which reaches it only through a Linux shim that Haiku itself could not use. Intel's firmware is not redistributable and lives on the disk, like the game data | docs/drivers.md, roadmap.md |
| Sep 2026 | **The compositor uses the vector unit, and keeping the branches is what made it worth 8x.** Compositing is 84.7% of a busy pass and it is one loop. The kernel may not touch an FP or SIMD register - `-mgeneral-regs-only` is what makes lazy FP save possible and is worth 29.9% of a context switch - but `gfx.c` is an EL0 process and pays the one fault per slice `arch/aarch64/fp.c` already accounts for. **Measured natively rather than under emulation**, because TCG translates each vector instruction and would have answered the wrong question. The finding was in the middle column: vectorising *without* `over`'s short circuits is **slower** for transparent pixels, 1546 against 2564 Mpx/s, since removing a branch that almost always wins costs more than the lanes save. So the fast paths are kept and asked of four pixels at once - all-opaque becomes a masked copy, all-transparent becomes nothing - and a window's interior goes from 1200 to 9865 Mpx/s, transparent 2564 to 7122, text and shadows 525 to 1293. GCC's vector types rather than intrinsics, so one source is NEON on the Pi, SSE2 on the ThinkPad and AltiVec on a G4. Checked against the scalar path over 33.5 million pixels at all four lane positions, because a fast path right for lane 0 and wrong for lane 3 is the bug this shape invites | user/lib/gfx.c, gfx.md §19.14 |
| Sep 2026 | **JPEG is vendored and PNG is not, and the disk is why either matters.** A 1920x1080 photograph is about three megabytes as a PNG and three hundred kilobytes as a JPEG, against a 32 MB disk - nine wallpapers or ninety. PNG stays ours: an inflate and five row filters, readable in an afternoon, and the right format for icons and the test pattern, which are flat colour with sharp edges and an alpha channel. JPEG is a discrete cosine transform, a Huffman decoder, four chroma layouts, progressive scans and restart markers, and `png.c`'s own rule turns around on it - *a decoder that half-supports a format is worse than one that says no*, and a baseline-only decoder fails on somebody's wallpaper rather than on a test. So `stb_image.h` joins `stb_truetype.h` in `runtime/upstream/stb/`, same author, same licence, unmodified, instantiated once with `STBI_ONLY_JPEG` so it is not a second PNG decoder nobody exercises. `STBI_NO_THREAD_LOCALS` is not a preference: stb keeps its failure string in a `__thread` variable and GCC answers that on bare metal with `__emutls_get_address`, which this system does not link. Reached as `gfx.jpeg` beside `gfx.png` rather than as a kit - a kit was defensible and the caller decided it, because `picture_from_file` opens a picture and should not have to know that one format is on a table and the other behind a `use`. Chosen by extension rather than by sniffing the signature: a file called `.png` that is not one is a mistake worth reporting | user/lib/jpeg.c, runtime/upstream/stb/, gfx.md §19.15 |
| Sep 2026 | **An emptied hook is not an empty hook, and the Terminal repainted once a second for nothing.** `pump` was a 0x0 view whose `tick` had been emptied when its work moved into `on_frame` - the right move - and the empty function was left behind. `window:add` tests whether a child *has* a `tick`, because one that does means "this changes on its own, like a clock", so the window was given a one-second `tick_every` and the run loop marked it dirty on that clock whether or not anything had happened: the whole window, banner and every run, re-sent to the compositor for ever with nothing to say. That is also the largest frame any window here sends, and the compositor writes a surface on every batch while holding only the *damage* to the last - so it is the widest opening for something else's damage to scan out a terminal that has been cleared and not yet re-texted, which is what it looked like from the outside. The same file also pinned `poll_wait_ticks = 1` from open to close, waking sixty times a second to serve children it did not have; it is raised while somebody might be blocked on an answer and dropped otherwise, and typing never depended on it because a held poll is answered the moment the window manager has an event | user/bin/terminal.lua, user/lib/ui.lua |
| Sep 2026 | **A widget that does not say it follows an edge does not follow it, and the Terminal never said.** Its view was built with the width and height of the window it was opened at, so a resized Terminal kept the grid it started with and the window's colour around it - the arithmetic in `draw` was right all along and divides `self.w` by the cell every pass. `follow` on all four edges is the whole fix, and the test that proves it drags the sizing grip and measures the console rectangle: 0x0 without the flag, 290x144 with it, which is exactly the room the screen had | user/bin/terminal.lua |
| Sep 2026 | **The keyboard declares itself, because a hand-written list of shortcuts is a copy that goes stale in silence.** `docs/cheatsheet.html` is a good document and it listed none of the five Super keys - it was written before they existed, nothing failed, and nobody was told. So `wm.lua` holds one ordered array of bindings, each with the sentence that describes it, and *builds its dispatch table from that array*: a shortcut cannot be added without saying what it does. A `shortcuts` request returns the list and the Shortcuts window draws it, so the window cannot disagree with the keyboard because there is only one of them. The window manager answers because the window manager decides - these keys are taken before any application sees them, so a list compiled anywhere else would be a guess about another process's behaviour. Super+Tab joins them, going round by raising the bottom window, which also fixed Control-W Tab: it did `raise(windows[1])`, and `windows[1]` became the backdrop the day the desktop gained one - which `raise` refuses by design, so it had done nothing for months. And the launcher pad opens centred, `centre` being a flag on the open request rather than arithmetic in the application, because an application has no framebuffer and does not know how big the screen is | user/bin/shortcuts.lua, user/bin/wm.lua, user/lib/ui.lua |
| Sep 2026 | **Control-C is copy, and ending the desktop moved behind the prefix.** The row above put the clipboard on `Control-W a c x v` because there were no modifier keys and Control-C already stopped a program. Both halves stopped being true. **A modifier arrived** - `hal/keys.c` reads Windows on a PC and Command on an Apple keyboard, the same HID usage, and carries it up as an escape sequence - so the window manager has its own place for a command and need not borrow one. And **Control-C ending the desktop was never a decision**: it was the first line of `key`, before anything could look at the byte, dating from a window manager with no applications in it. What that meant in practice is what got reported - pressing copy closed the whole desktop. So Control-A/C/X/V are the four everybody's fingers already know, `Control-W Q` ends the desktop because that is the most destructive thing this keyboard can do and deserves two deliberate presses, and `Control-W C` sends a literal Control-C through - which nothing needs yet, since the Terminal has never interrupted a child, and which exists so the key is reachable on the day it does. **Lite XL needed not one line changed**, because it reads the *intent* rather than the key: a window told `{type = "copy"}` does not know which key produced it, which is the whole point of sending intent rather than text. The eighteen display checks that pressed Control-C to hand the screen back are now one named constant rather than eighteen copies of the bytes | user/bin/wm.lua, ui.md §16.11, tools/run_screenshot.py |

---

## Language

**Everything in this repository is written in English.** Documents, source code, comments, commit messages, identifiers, test names, log output. No exceptions.
