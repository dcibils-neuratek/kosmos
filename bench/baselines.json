{
  "context_switch": {
    "commit": "pending",
    "iterations": 200000,
    "note": "One of the three that touch no Lua. 9.812 -> 6.875, -29.9%, when the FP and SIMD register file stopped being saved and restored on every switch. The switch now only disarms FP (CPACR_EL1.FPEN) and the first floating-point instruction after it faults into `fp_fault`, which moves the registers to whoever wanted them. A thread that never touches FP - which is every kernel thread, since the kernel is built -mgeneral-regs-only - pays nothing at all. See arch/aarch64/fp.c.",
    "tol": 0.02,
    "unit": "counter ticks per operation, QEMU -icount shift=0",
    "value": 6.875
  },
  "exception": {
    "commit": "pending",
    "iterations": 20000,
    "note": "The cost of taking and returning from a synchronous exception. 7.250 -> 7.562, +4.3%, and it is the price of lazy FP save rather than a regression to chase: every exception now begins by asking whether it is an FP trap, because that question has to be answered before anything else looks at the frame. One comparison, paid on every exception, to save the whole register file on every context switch - `context_switch` went down 29.9% and `ipc_roundtrip` 16.2% in the same change.",
    "tol": 0.02,
    "unit": "counter ticks per operation, QEMU -icount shift=0",
    "value": 7.562
  },
  "gc_pause_max": {
    "commit": "pending",
    "iterations": 1,
    "note": "The worst single collector step, measured in a process. 78221, drifting up with what the process carries in the same way serialize does and by far less: 78129 before gfx, 78199 after, 78221 with the font. A long step is dominated by the 3000-object heap the benchmark builds rather than by a handful of library tables. Before that it was 77860, running inside the kernel with interrupts masked. Leaving EL1 cost the interpreter +0.3% and nothing else: a collector step costs the same at either privilege level. Interrupts are live in the window now, because a process cannot mask them, and for a pause metric that is more honest rather than less - a pause the user feels includes the tick that landed in it.",
    "tol": 0.02,
    "unit": "counter ticks per operation, QEMU -icount shift=0",
    "value": 78371.0
  },
  "ipc_roundtrip": {
    "commit": "pending",
    "iterations": 100000,
    "note": "A minimal message: the floor of the IPC path, including the capability lookup. 36.251 -> 30.376, -16.2%, from lazy FP save: a round trip is two context switches and it was paying for the register file twice.",
    "tol": 0.02,
    "unit": "counter ticks per operation, QEMU -icount shift=0",
    "value": 30.376
  },
  "serialize": {
    "commit": "pending",
    "iterations": 20000,
    "note": "Best of five processes within a run. **This benchmark is sensitive to what else is in the process**, not only to the serialiser: every library the image opens is more objects for the collector to pace against. 1450.574 -> 1498.325, +3.3%, when the PDF scanner and the inflate kit joined the user image; `serialize.c` was untouched. If it moves and nothing in `serialize.c` or `sys_user.c` did, look at what was added. Making it independent of its process - a fixed GC pause and a forced collection before the loop - is a known improvement and is still not done.",
    "tol": 0.02,
    "unit": "counter ticks per operation, QEMU -icount shift=0",
    "value": 1498.325
  }
}
